# Mermaid 图格式保障方法论

在 AI 生成 PRD/TRD 等文档时,mermaid 图极易因模型排版随意而渲染失败。要让图"格式不乱、最终一定能渲染正确",核心思路是**构建一条从生成侧引导到消费侧兜底的多层确定性防御链**:把"模型可能写错"逐层收敛为"最终一定渲染正确",任何一层失守都有下一层补救。

## 一、生成侧:提示词强约束(治本)

在系统提示中把图表排版列为"硬性红线",从源头引导模型产出规范语法:

- 图表指令独占首行;
- 每个节点、每条连线各自换行,一行只写一条;
- 禁止把图表指令与节点挤在同一行,禁止把多条连线写在同一行;
- 配正确 / 错误对照示例,强化模型对"合法排版"的认知。

**要点**:治本在于源头引导,让大多数图一次生成即合规。

## 二、修复侧:专用语法修复器(补救)

对疑似排版错乱的图,单独引入一个"只修排版、不改语义"的修复角色:

- 只拆分挤成一行的连线、补齐缺失的围栏、规整逐行结构;
- 严格不动节点文字、不改连线方向、不增删节点、不换图表类型。

**要点**:把"自然语言层面的排版错误"交给擅长语言的一层专门矫正,与确定性规则形成互补。

## 三、文档侧:确定性正则规范化(兜正)

不依赖模型自觉,用规则强制修复块级结构:

- 把粘连的标题 / 表格 / 列表强制断行;
- 把挤成一行的语言标识与图正文拆开;
- 围栏内遇到标题自动补齐闭合围栏,防止未闭合围栏吞掉后续正文。

**要点**:确定性规则不受模型波动影响,是最可靠的一道结构性兜底。

## 四、探测侧:统一裸块识别

对未标注语言的代码块,用同一套关键字口径判定其是否为图表:

- 规范化层与渲染层共用一致的判断标准;
- 避免"该渲染的图因缺少语言标注而被当作普通代码"。

**要点**:统一口径消除层间认知偏差,防止"漏渲染"。

## 五、渲染侧:流式时序 + 引擎缺陷规避

在流式输出与并发渲染场景下控制时序、规避绘图引擎缺陷:

- 流式半截图先用骨架占位,待终稿完整块到达才真正渲染,避免半截语法报错与闪烁;
- 渲染串行化 + 结果缓存 + 失败残留清理,规避绘图引擎单例非并发安全的问题。

**要点**:把"时机不对"和"库自身缺陷"这两类非语法问题也纳入防御。

## 一句话总结

生成侧规范引导 → 修复侧语义无损矫正 → 文档侧确定性正则兜底 → 探测侧统一识别 → 渲染侧时序控制与引擎规避,**层层收敛不确定性**,把"模型可能写错"转化为"最终一定渲染正确"。