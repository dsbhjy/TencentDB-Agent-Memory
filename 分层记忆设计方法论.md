# 分层记忆设计方法论

- 日期：2026-09-03
- 来源：L0/L1 设计与落地实践提炼
- 性质：可复用方法论，脱离具体业务场景，任何"Agent + 长期记忆"系统可照此设计
- 配套：[分层记忆设计模板.md](./分层记忆设计模板.md)、[记忆系统L0-L3分析.md](./tencentdb实现逻辑.md)（TencentDB L0-L3 机制参考）

---

## 0. 这份方法论回答什么

> 当你要给一个 Agent 系统加"长期记忆"时，怎么设计才不会变成一锅混乱的日志、不会污染上下文、不会反噬主流程？

核心论点：**记忆系统要分层，每层职责单一、作用域明确、可独立演进。分层的第一目标不是"记住更多"，而是"在不同粒度上安全复用"。**

---

## 1. 分层原则

### 1.1 四层模型

| 层 | 作用域 | 职责一句话 | 形态 |
| --- | --- | --- | --- |
| L0 | turn / requirement | 原始证据：干净用户输入 + 助手完整产物 | 原文 |
| L1 | requirement | 原子记忆：从 L0 抽取的可复用事实/决策/约束/风险/待确认 | 结构化条目 |
| L2 | requirement / 相似需求 | 需求画像：模块地图、上下游、风险模式（可选，见下） | 聚合视图 |
| L3 | user | 用户偏好：跨需求稳定沉淀的写作风格、关注点 | 稳定模式 |

> **L2 可选**：L1 已是结构化原子，L3 可直接从多个需求的 L1 提炼，不需要先聚合成 L2 画像。「需求画像」可作为 L1 的运行时聚合查询，不独立存储。**若无跨需求相似召回需求，L2 可取消**，分层简化为 L0→L1→L3。只有需要「新需求找相似历史需求」时，L2 作为召回索引才有独立价值。

### 1.2 分层的根本理由

不分层的失败模式：
- 把原始对话直接塞回 prompt → 上下文爆炸、token 失控；
- 把单个需求的事实当用户偏好 → 跨需求污染；
- 没有作用域 → A 需求的记忆串到 B 需求，模型答非所问。

分层解决三件事：
1. **粒度匹配**：不同消费场景需要不同粒度的记忆（审计要原文，生成要结构化，跨需求要画像）；
2. **作用域隔离**：requirement 作用域和 user 作用域必须显式区分，防止污染；
3. **独立演进**：每层可独立上线、独立降级、独立验证，不必一次做完。

### 1.3 优先级铁律

> **先解决"同一个需求内跨模块同步"，再解决"同一个用户跨需求偏好复用"。**

跨需求偏好（L3）必须从多个需求（L1/L2）中反复出现的稳定模式提炼，**绝不能把单个需求的事实直接升格为用户偏好**。否则一个需求的细节会污染另一个需求的生成。

---

## 2. L0：原始证据层

### 2.1 定位

L0 是记忆的源头数据层，只做"记录"，不做"理解"。但要保证记录的是**干净原文**。

### 2.2 设计要点

1. **存干净产物，不存原始请求**：
   - user 侧只存真实用户输入（剔除 system prompt、已注入的记忆、RAG 检索原文、harness 包装噪声）；
   - assistant 侧优先存"完整产物"（如生成的文档 markdown），而不是模板提示语（如"文档已写到右侧"）；
   - 这是防反馈循环的核心：**已注入的记忆不能当成用户说的话再存一遍**。

2. **幂等写入**：
   - 用 `turn_id = env + user + session + run` 做唯一键；
   - 同 run 重放、重试、SSE 断连重连不产生重复记录；
   - 辅助指纹（如 SHA-256(text)）处理"同文本反复追问"。

3. **旁路捕获，绝不反压主流程**：
   - L0 写库用专用有界线程池异步执行；
   - 队列满、写库失败、数据源缺失 → 只记日志，不阻断 Agent/SSE；
   - 主流程优先，L0 尽力而为。

4. **生命周期独立**：
   - 业务会话 reset / 删除不删 L0（L0 是"已发生的对话事实"）；
   - 与业务会话历史表、模型侧短期记忆表分开，避免被业务操作误删。

5. **下游触发预留**：
   - L0 行带 `notify_status`（pending/done/skipped/failed），供 L1 消费扫描；
   - L0 是事件源，L1 异步消费它。

### 2.3 反模式

- ❌ 把 `request.context` 整个 JSON 落库正文（前端页面上下文是噪声）；
- ❌ 用业务会话历史表当 L0（会被 reset 删、内容未清洗）；
- ❌ 用模型侧 ChatMemory 当 L0（带窗口会丢、是模型上下文不是业务记忆）；
- ❌ L0 写库失败让 Agent 请求失败（反压主流程）。

---

## 3. L1：原子记忆层

### 3.1 定位

L1 不是"聊天摘要"，而是从 L0 长文本里抽取的**可被后续模块稳定复用的结构化原子**。

### 3.2 记忆类型设计

推荐六类，覆盖需求演进的完整生命周期：

| 类型 | 含义 | 何时抽取 |
| --- | --- | --- |
| fact | 客观事实 | 描述需求背景、现状 |
| decision | 已定决策 | 已明确的选择（一期做什么、用什么方案） |
| constraint | 约束/口径 | 边界、限制、过滤规则 |
| risk | 风险 | 不确定性、潜在问题 |
| open_question | 待确认项 | 未决问题，需后续确认 |
| task_hint | 任务提示 | 后续可拆解的模块提示 |

关键约束：
- **每条 atom 必须可追溯到 L0**（source_l0_id + source_turn_id 硬校验）；
- **不确定的事项用 open_question，不要伪装成 fact**；
- **不存大段原文**（整篇 PRD 仍在 L0，L1 只存提炼后的短表达）。

### 3.3 抽取设计

1. **L0 作为事件源，L1 异步消费**：扫 `notify_status=pending` 的 L0，调模型抽取，upsert L1，回写 L0 状态；
2. **模型输出不可信，应用层做机械防护**：
   - memory_type 白名单（只接受预定义类型）；
   - 必填校验（subject/content 非空）；
   - 大段原文防护（超长或含多个 markdown 标题 → 判定为产物碎片，丢弃）；
   - 可追溯性硬约束（缺 source_l0_id 直接抛异常）；
   - 作用域硬约束（scope 强制 requirement，防越界）。
3. **prompt 约束 subject 规范化**：要求模型用简短主题词、同主题统一表述，从源头降低同义变体。

### 3.4 去重设计

去重是 L1 的核心难点，分两步：

1. **文本归一化去重**：
   - `dedup_key = hash(env + requirement + memory_type + normalized(subject))`；
   - **不要把 predicate 纳入去重键**——predicate 是模型自由生成的不稳定谓词，同事实换 predicate 就逃逸去重；
   - normalized = 去首尾空白 + 折叠连续空白 + 转小写。

2. **残留的近义变体**：
   - "context信息时效性" vs "context时效性" 这种 subject 措辞差异，文本归一化搞不定；
   - 需语义相似度判断，但风险高（易误并不同事实），MVP 阶段宁可留重复也不要误并。

### 3.5 注入设计（关键决策点）

> **L1 注入是双刃剑：在"前序原文已注入"的场景下，L1 与原文信息冗余，叠加注入可能让模型产生"信息已充分"的错觉，输出反而更简略。**

注入前必须想清楚：L1 注入的目标场景是什么。三种"注入"路径都已被实验证明有问题：
- **补充原文**：原文已在 prompt，L1 叠加 → 经验证（B vs A）边际贡献负向，信息冗余；
- **替代原文**：原文过长被截断 / 不在场时，用 L1 撑起关键信息 → 曾以为是价值场景，但 D2 vs D3 对照证伪：L1 独有表述没进产物，模型是从其他上下文自撑的；
- **跨模块/跨需求复用**：下游模块（Task/Review）本就不读前序长文，L1 结构化记忆更契合 → 但同样面临"注入是否被模型采用"的待验证问题。

> **更可靠的结论：L1 作为"生成时注入的 prompt 段落"这条路难被模型采用——位置对照实验证实，L1 无论放末尾还是开头，模型都不采用其具体信息。根因不是淹没，是形态（普通 markdown 列表 + 缺强指令）被模型当成可选背景。结构化记忆的真正价值场景是"生成后校验"——用 L1 检查产物是否丢了关键决策/约束/待确认项，这绕过了"模型是否采用"的不确定性，且最契合结构化记忆的形态。详见 §4.5 的四组实验结论。**

### 3.6 注入的故障隔离（fail-open）

L1 是旁路，绝不能拖垮主链路：
- 仓库不可用 → 查询返回空，注入无感跳过；
- 运行时异常 → catch + warn，返回空串，主流程照常生成；
- **L1 出问题只会让生成"没有记忆参考"，不会让生成失败。**

### 3.7 反模式

- ❌ L1 存整篇 PRD 摘要（那是 L0 的职责）；
- ❌ 把单个需求事实抽成用户偏好（scope 越界）；
- ❌ 注入侧不做 fail-open，DB 异常导致生成失败；
- ❌ 去重键纳入不稳定字段（predicate）。

---

## 4. 验证方法学

### 4.1 不要用混淆判据

> **"产物承接了前序文档"不能证明记忆生效**——因为前序原文可能也在 prompt 里，模型从原文就能读到。

这是记忆系统验证最常见的陷阱。任何验证都必须先回答：**这个现象在有记忆和没记忆两种情况下都会发生吗？**

### 4.2 指纹日志法

为注入点加指纹日志，记录：是否注入、记忆长度、是否含独有标记（如特定标题）。独有标记是排除原文干扰的直接判据——原文不会有这个标记。

### 4.3 A/B/C 对照法

用开关控制变量，做三组对照：

| 组 | 记忆 | 前序原文 | 目的 |
| --- | --- | --- | --- |
| A | 关 | 开 | 基线：只有原文 |
| B | 开 | 开 | 记忆 + 原文是否叠加有正贡献 |
| C | 开 | 关 | 记忆能否替代原文 |

- A vs B：判断记忆在原文在场时的边际贡献；
- C：判断记忆独立支撑的能力；
- **所有开关状态用指纹日志逐组确认**，不靠假设。

### 4.4 诚实对待结论

- 机制验证（注入确实发生）和质量验证（注入让产物更好）是两件事，不能混为一谈；
- 对照实验若显示无正贡献，要如实记录，并重新定位记忆的价值场景，而不是强行宣称成功。

### 4.5 一次真实 A/B/C/D 实验的结论（PRD→L1→TRD 场景）

同一需求下，用注入开关做四组对照，所有开关状态用指纹日志逐组确认：

| 组 | L1 记忆 | PRD 原文 | 指纹日志 | TRD 产物 |
| --- | --- | --- | --- | --- |
| A | ✗ | ✓ | `injected=false` | 25282 字符 |
| B | ✓（885 字符） | ✓ | `injected=true memoryLen=885` | 24688 字符 |
| C | ✓（885 字符） | ✗ | `injected=true` + `PRD disabled` | 22711 字符 |
| D2 | ✗ | ✗ | `L1 disabled` + `PRD disabled` | 15105 字符 |

D2 vs D3（即 C）是 isolate L1 单独贡献的最干净对照：两组都没 PRD，唯一差异是 D3 多注入 885 字符 L1。

**结论分四层，不能混为一谈：**

1. **机制验证（成立）**：四组指纹日志逐组确认，开关变量干净，L1 注入确实发生且内容是结构化记忆（含独有标记），排除了原文干扰。

2. **叠加注入无正贡献（B vs A）**：前序原文已在 prompt 时，叠加 L1 对产物没有正向贡献，反而轻微负向。L1 是原文的结构化复述，信息冗余，叠加后模型产生"信息已充分"的错觉，输出更简略。

3. **L1 没有替代原文的能力（D2 vs D3，最关键）**：D3（有 L1）比 D2（无 L1）没有任何关键词多出来；L1 独有表述（如「七级格子」）在 D3 产物里出现 0 次，与 D2 相同。说明 C 组产物没崩不是 L1 撑起的，是模型从其他上下文（IDEA/context JSON）自撑的（D2 什么都没有也能撑 15105 字符）。**这推翻了"L1 能替代原文"的乐观判断。**

4. **根因不是注入位置/淹没，而是形态与对齐（位置对照实验证实）**：曾以为 L1 被大段 context JSON 淹没，但把 L1 从 prompt 末尾改注入到开头（l1MarkerIndex=5）重跑后，L1 独有表述（`(1-5]` `(5-10]` `30-+∞` `勾选RT` `七级格子`）在产物里仍为 0 次。说明「淹没/位置」不是主因——模型读到了 L1（指纹+快照证实），但主动不采用其具体信息。真正根因是：L1 用普通 markdown 列表注入、缺强指令，被模型当成可选背景而非必须遵循的约束；且 L1 是原文的结构化复述，没有独占信息能强制改变输出。

> ⚠️ **第 4 层结论已被推翻（见 §4.6）**：上述判据（「产物里是否出现 L1 具体符号」）是错误的——模型连 PRD 原文都不逐字保留，是「理解后重写」。用逐字符号判据，连 PRD 本身都会被判无效。用正确的语义盲评判据，L1 代替 PRD 价值成立。

**这次实验最重要的发现（已修正）：**

> **L1 代替 PRD 做上下文注入，价值成立。** 用 LLM 盲评（语义覆盖判据）验证：L1 用 PRD 约 26% 的篇幅，驱动出信息覆盖率 93%、综合质量 85-90% 的 TRD，且在监控公式/脱敏规则/对账补偿等维度反而比有 PRD 更具体。详见 §4.6。

这也是 §3.5 注入设计要提前想清楚的原因——评估「注入是否有效」必须用语义判据（产物是否在语义上覆盖关键信息），不能用逐字符号判据（模型对 PRD 和 L1 都是理解后重写，不保留原符号）。

### 4.6 判据纠错与 L1 代替 PRD 的正面结论（2026-09-04）

**§4.5 第 4 层「L1 注入无效」的结论建立在错误判据上，已推翻。**

错误判据：用「产物里是否出现 L1 的具体符号（如 `(1-5]` `30-+∞` `七级格子`）」判断 L1 是否被采用。

证伪：有完整 PRD 的基线组产物里，PRD 原文有的这些具体符号也全是 0 次。**模型连 PRD 原文都不逐字保留，是「理解后重写」而非「复制粘贴」。** 用逐字符号判据，连 PRD 本身都会被判无效，判据不成立。

**正确判据 + 干净对照实验：**

为排除 ChatMemory 污染（同 session 会累积历史对话），用两个独立需求各跑一组（新需求新 session，ChatMemory count=0 起步）：

| 组 | prompt 输入 | 产物 | ChatMemory |
| --- | --- | --- | --- |
| 基线A | 有 PRD（2865 字符）+ 无 L1 | 10923 字符，12 章节 | 干净 |
| L1组 | 无 PRD + 有 L1（738 字符，13 条 atom） | 8461 字符，12 章节 | 干净 |

LLM 盲评（裁判不知哪份是 L1 组）结论：

> **L1 代替 PRD 注入上下文生成 TRD，信息覆盖率达 93%，综合质量达 85-90%，且部分维度反而更优。**

关键发现：
1. **信息覆盖率**：基线A 100%，L1组 93%。唯一遗漏是「关键路径 99.95%」未区分，影响有限；
2. **L1 组在多个维度反而更具体**：监控指标（公式+告警阈值）、脱敏规则（具体替换规则）、对账补偿（定时对账+工单）、排期（具体周数）均优于有 PRD 的基线A。说明 L1 里结构化的约束/决策足以驱动模型生成具体技术方案；
3. **压缩比**：L1 用 738 字符（约 PRD 26% 篇幅），驱动出质量 85-93% 的 TRD。

**结论：L1 代替 PRD 做上下文注入，价值成立。** L1 作为 PRD 的结构化压缩，表现出「信息密度高、损失低」的特点，在上下文窗口受限或批量生成场景下有明确实用价值。

**方法论层面的教训：**
- 评估记忆注入是否有效，**必须用语义判据**（产物是否在语义上覆盖关键信息），不能用逐字符号判据；
- 对照实验必须**排除 ChatMemory 污染**（用不同 session 或新需求），否则模型从历史对话自撑，掩盖记忆的真实贡献；
- 负面结论（如「L1 无效」）在判据错误时同样会被误导，需用正确判据复核；
- **「关 PRD 注入」不等于「模型看不到 PRD」**：请求的 context JSON 里可能仍带 PRD 字段，会漏给模型。必须同时剥离 context 里的 PRD 字段，才能真正 isolate 记忆的贡献。这是最容易遗漏的污染源。

**最终干净对照结论（彻底剥离 PRD 后，prompt size 准确测得）：**

> L1 代替 PRD 做上下文注入，价值成立。真实质量损失约 10-15%（集中在数值精度和领域专有概念），token 节省约 42%（14211→8246），产物体积几乎相同。L1 省的是「PRD 注入段 + context 里的 PRD 字段」这部分 token，核心架构与 PRD 组 100% 一致。损失可通过 L1 数值高优标记 + 生成后数值校验补偿。

注意：此结论基于泛化 PRD（模型推断的）。若真实 PRD 含高度领域特异性业务规则/精确数值，L1 损失可能更大，需用真实业务 PRD 复验。

---

## 5. 工程实现要点

### 5.1 数据源隔离

- 记忆库独立于业务库（独立 schema / 独立数据源），避免业务库故障影响记忆，反之亦然；
- 用 `ObjectProvider` 注入数据源，非目标环境（如生产未配置）静默降级。

### 5.2 配置开关

每个记忆能力都加开关，默认行为可降级到"关闭"：
- 捕获开关、注入开关、原文注入开关分开，便于实验和灰度；
- 开关支持运行时切换最佳（避免实验时频繁重启）。

### 5.3 异步与限流

- L0 捕获、L1 消费都是旁路，用专用有界线程池；
- 队列满丢弃 + warn，不反压；
- L1 消费逐条调模型，耗时长，线上化需改后台批量 + 断点续做。

### 5.4 env 隔离

- 所有记忆数据按 env（dev/pre/prod）隔离；
- env 由当前 profile 归一化，不要硬编码（否则线上化踩坑）。

---

## 6. 落地节奏

1. **先 L0，再 L1，最后 L2/L3**：每层独立可上线，L0 不依赖 L1；
2. **每层先做闭环验证再做优化**：L0 先验证"干净落库"，L1 先验证"抽取 + 注入机制"，再验证"质量贡献"；
3. **MVP 只追一个核心闭环**：如 PRD→L0→L1→TRD 注入，跑通再扩展；
4. **不做的不碰**：向量化、图谱、人工编辑面板、强可靠 MQ，留到闭环跑通后。

---

## 7. 核心原则汇总

1. **记忆分层，每层职责单一、作用域明确。**
2. **L0 只记录不理解，但必须是干净原文。**
3. **L1 是结构化原子记忆，不是聊天摘要。**
4. **每条记忆必须可追溯到来源。**
5. **单个需求事实不能直接升格为用户偏好。**
6. **记忆是旁路，绝不反压主流程。**
7. **模型输出不可信，应用层做机械防护。**
8. **验证用指纹 + 对照，不用混淆判据。**
9. **先需求内同步，后跨需求偏好。**
10. **没有正贡献就如实记录，重新定位价值场景。**
