# ERP 鉴权实现方法论

在企业内部系统里,鉴权需要回答两个不同的问题:**"你是谁"(身份识别)与"你能做什么"(权限授权)**。核心思路是**把这两层职责彻底解耦**,并把"是否登录"的门禁前移到网关/探针层,后端只做身份识别与业务授权。

## 一、身份识别:多来源优先级解析(你是谁)

后端不实现自己的登录会话,而是从当前请求上下文按优先级"就近取用"用户身份:

- **凭证优先**:先读约定的身份 Cookie;取不到时解析登录令牌(JWT)——解码其 payload,从内嵌的用户字段中提取身份标识。
- **请求头兜底**:凭证缺失时,退而读取一组约定的自定义请求头(由上游网关注入)。
- **最终兜底**:全部取不到时返回匿名身份,并显式标记为"mock/未识别",供上层区分真实用户与兜底态。

**要点**:身份识别是"尽力而为"的多源合并,而非强校验;来源优先级明确、可回退,兼容本地开发、预发、线上多套环境。

## 二、门禁前移:登录态由前端探针负责

真正的"是否已登录"判定不放在业务后端,而是前移到前端网关 / 登录探针层:

- 后端默认信任上游已完成登录校验,自身不做登录拦截;
- 业务代码专注于"拿到身份后做什么",职责单一。

**要点**:门禁与业务解耦,避免每个接口重复写登录校验;登录逻辑集中在一处,便于统一升级与灰度。

## 三、权限授权:按资源维度的分级角色解析(你能做什么)

授权以"资源 + 用户"为维度解析角色,而非全局粗粒度开关,按优先级逐级判定:

- **超级管理员(白名单,全放行)**:由配置中心维护,最高优先级;
- **资源属主(owner)**:资源归属人天然拥有管理权;
- **成员角色(编辑者 / 查看者等)**:由成员关系表维护;
- **角色到权限点的映射**:由独立的角色权限表决定,角色与具体操作权限解耦。

**要点**:分级短路——命中高优先级即返回,兼顾灵活与性能;角色与权限点分离,便于扩展新操作而不改鉴权主干。

## 四、性能与一致性:短 TTL 缓存 + 写侧主动失效

授权解析涉及多次库查询,用缓存兜住热点:

- 结果按"资源 + 用户"维度缓存,设较短 TTL,避免授权变更长时间不生效;
- 成员或属主变更时,由写侧主动使相关缓存失效;
- 用占位符区分"未缓存"与"已缓存但无权限",避免缓存穿透。

**要点**:短 TTL + 主动失效双保险,在"授权及时生效"与"减少库压力"之间取平衡。

## 五、外部系统鉴权透传:复用用户登录态

当后端需以用户身份调用外部系统(如企业文档)时,不重新登录,而是透传用户既有凭证:

- 从请求 Cookie 抽取单点登录令牌、完整 Cookie 头等;
- 附加到对下游的请求中,复用用户登录态完成调用。

**要点**:以用户身份而非系统身份访问外部资源,天然继承外部系统的数据权限,避免越权。

## 一句话总结

前端探针管登录门禁 → 后端多源(凭证 / 令牌 / 请求头)识别身份 → 按资源维度做超管 / 属主 / 成员的分级授权(短 TTL 缓存 + 写侧失效)→ 调外部系统时透传用户凭证,**身份与权限解耦、门禁前移、授权分级**,构成一套轻后端、可回退、易扩展的鉴权体系。