# 流式输出实现方法论

本项目基于 **SSE（服务器推送事件）** 构建端到端流式输出，其设计可抽象为四条核心方法论。

## 一、分层解耦，让每层只关心自己的粒度

- **模型层**：把大模型 SDK 返回的响应式数据流逐块消费，通过回调把「正文增量」与「思考内容」分离——思考不混入正文，避免污染下游解析。
- **传输层**：Web 层不感知模型细节，只负责把每个增量包装成语义化事件推送给浏览器。
- **前端层**：只按事件类型分发渲染，不理解模型如何产生内容。

各层通过回调 / 事件边界通信，任一层可独立替换或重试。

## 二、生成与连接彻底解耦（可靠性核心）

- 生成任务放入独立线程池执行，**不绑定 HTTP 连接生命周期**。
- 客户端断线时，服务端只标记「已断连、停止推送」，**绝不中断生成、也不抛异常**，让任务跑完并完整落库。用户重连后拉历史即可看到完整结果——保证「用户走开也不丢结果」。

## 三、用语义化事件通道区分内容去向

不把所有内容混在一条流里，而是拆成多种事件：起始、正文增量、思考、结构化元信息、工具调用、完成、失败等。前端据此把内容**精准路由**到不同 UI 区域（如左侧对话气泡 vs 右侧文档预览），意图判定由服务端下发，前端不自行猜测。

## 四、真流式 + 性能 / 正确性的工程权衡

- **打通链路**：关闭反向代理缓冲，确保逐块即时抵达浏览器，实现真正的打字机效果。
- **首字节可观测**：埋点首字节到达时刻，用于区分「连接慢」还是「模型陷在思考」。
- **有界重试**：仅在「尚未吐出任何内容」时重试，已开始输出则直接回落，杜绝重复内容；上下文超限时先压缩再重试一次。
- **节流防洪水**：高频增量帧用时间门控合并（约每 60ms 一次），避免渲染风暴；同时保证末帧强制冲刷，不丢尾部内容。
- **时序兜底**：元信息未到时先缓冲增量、待落定后回放，避免首字渲染到错误位置。
- **中断可续**：流中途异常但已产出内容时标记为「可续写」，为断点续传留出空间。

## 一句话总结

以 SSE 为骨架，通过「分层解耦 + 生成与连接分离 + 语义化事件路由 + 缓冲 / 节流 / 重试 / 续写等工程兜底」，在保证实时打字机体验的同时兼顾了可靠性、性能与内容正确性。