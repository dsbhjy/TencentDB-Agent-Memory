# 分层记忆实验方法学

> 本文档回答一个面试官几乎必然追问的问题:**「token 省 42%、质量损失 10-15%」这些量化证据是怎么测的?依靠什么原则?**
>
> 目标:把「控制变量 + 盲评 + 可复现」的测量方法讲清楚,并坦白边界——数据不是从天上来的,踩过的坑(隐藏泄漏)反而是最硬的谈资。

---

## 一、三个核心数据分别怎么测

### 1. token 省 42%(14211 → 8246)

**测量工具**:每轮 TRD 生成时,`AgentPromptComposer` 组装完 prompt 后打一行 `prompt size` 日志,含 `userChars / userTokens / systemTokens`。两组都从这行日志取数。

**测量原则(最关键)**:要测 token 差异,必须保证**两组除了 PRD/L1 段,其他一模一样**。否则这不是「L1 的作用」,而是「两个需求本来 context 就不同」。§7.10 把变量锁死到三个:

1. **同需求**(都用 REQ-T-2026-000230)→ context JSON / IDEA / 附件 / 用户输入完全相同;
2. **强制随机新 session** → ChatMemory count=0,两轮不串;
3. **剥离 context 里的 `prd` 字段**(新增 `agent.strip-prd-from-context` 开关)——最重要的一步。

为什么第 3 步关键?之前几轮(§7.8/7.9)「token 省 45%」是**假象**:`trd-prd-inject=false` 只关了显式 PRD 段,但 context JSON 里仍带完整 PRD sections,模型偷偷看到了。直到加 strip 开关,让两组**真的只差「PRD 4558 字符」vs「L1 963 字符」**,得到的 42% 才可信。

因此差值干净:**基线A userToken 14211, L1组 8246, 少 5965(42%)**。这个差值 = 被剥离的 PRD(显式注入段 + context 里那份)。

### 2. 质量损失 10-15%

**测量工具**:LLM 盲评。裁判模型在**不知道哪份是 L1 组**的前提下,对两份产物五维度打分(1-5):架构完整性 / 技术决策具体性 / 可执行性 / 异常边界 / 方案深度。§7.10 均值:**产物X 3.7、产物Y 3.9**(反超)。

**为什么「10-15%」是归纳的,不是算出来的**:LLM 产物没法做引用级自动比对。裁判读两份 TRD,数出「X 承接 ~90% 关键信息、Y 承接 ~75-80%」,再归纳损失**集中在具体数值精度**:

- 可用性 99.9%/99.95% 分级 → 被合并成 99.99%(不区分);
- 审计保留 90 天 → 写成 180 天;
- 状态枚举偏离。

合并估 10-15%:**「L1 承接不了的 10-15%」= 精确数值、特定枚举、领域专有概念**(L1 压缩后信息损耗),不是「质量差的 10-15%」。L1 承接的 85%+ 是**架构骨架**(模块/流程/状态机/异常框架/配置/监控/测试),与 PRD 组完全一致。

### 3. 产物体积几乎相同(10016 vs 9848)

直接数产物结构与字符数。同需求下两组 TRD 结构一致——证明**省 token 是压输入,不是砍输出**。

---

## 二、整套实验遵循的测量原则(面试口径)

> 「实验遵循**控制变量 + 盲评 + 可复现**三原则:
> 1. **锁定混淆变量**——同需求(消除 context/IDEA/附件差异)、强制随机新 session(消除 ChatMemory 污染)、剥离 context 里的 `prd` 字段(堵住「关 PRD 但模型仍偷看 PRD」的泄漏)——两组唯一差异就是 PRD 段 vs L1 段;
> 2. **token 侧实测**——每组跑完立刻读 prompt-size 日志,不靠估算;两轮都读到才对比,未失日志覆盖;
> 3. **质量侧盲评**——裁判不知哪份是 L1 组;复验用三方裁判一致才定论;
> 4. **不混指标**——澄清「信息覆盖率 / 方案质量 / 产物体积」是不同指标,分别用不同方法测,不混用。」

---

## 三、两条必须主动交代的边界

1. **这个「42%」只在「PRD 全量注入」这个场景成立**;且本次 PRD 是泛化 PRD(模型推断、含 TODO,见 §7.10 局限)。若真实业务 PRD 含高度领域特异性参数/精确数字,L1 损失可能更大。**别把「特定实验的 loss」吹成全局 loss。**
2. **质量评估是模型裁判,非随机对照精确值**——「接近等同」和「方案深度 4.0 vs 3.5」含主观。所以简历上**只讲实测的 token 42%**,质量损失降级为「集中在数值精度」,把主观与客观分开。

---

## 四、一次最有价值的教训(谈资)

最严重的坑是 §7.8/7.9:「质量损失 0-7%」。那轮只关显式 PRD 段,但 context JSON 仍带 PRD——**L1 组模型偷偷看到了 PRD**,数据全是假象。§7.10 补齐泄漏路径(剥离 context 的 prd)重测,真实损失才定在 10-15%。

**这条教训本身就是面试加分项**:它证明你是一个「发现自己的数据有隐藏泄漏 → 主动修掉 → 重测」的人,而不是一报出好看数字就行。叙事:「我推翻过自己的漂亮数据——因为发现测量有泄漏,就把实验重新做干净。」

---

## 五、与本仓库文档的对接

- 原始数据与完整字段:见 `jkc-copolit-engine/docs/superpowers/specs/2026-09-02-memory-l1-design.md` §7.10 / §7.11(含 L0 id、session、指纹、字符/token、产物、模型、maxTokens、24条 atom 构成)。
- 简历侧口径:只引 token 42%(可测),质量讲「集中在数值精度」(不引绝对分)。

---

*面试前把这页过一遍即可;数字的原始出处永远指向 L1 设计文档 §7.11。*