# 个人简历(秋招)

> **求职意向**:AI 应用开发 / Java 后端
> **说明**:姓名、联系方式、院校占位,提交前替换为真实信息。

---

## 【基本信息】(提交前填写占位:姓名 · 手机 · 邮箱 · 院校 · 学历 · 毕业届 · 意向城市)

---

## 教育背景

**[院校占位]** · 本科/硕士 · **[毕业届]届毕业** · 主修/专业:[计算机科学与技术 / 软件工程]

- 相关课程:Java 程序设计 / 数据库 / 操作系统 / 计算机网络
- (可加:成绩排名 / 奖学金 / 项目经历)

---

## 项目经历

### 一站式 AI 需求协作平台(Web 应用 + 桌面端,个人主导后端核心)

**技术栈**: Java 17 / Spring Boot / Spring AI /  React 18 / TypeScript / Vite / Tailwind

**角色与职责**:参与从 0 到 1 落地后端核心 + Agent 编排 + 前端全链路。

**项目概述**:把「需求从创意到交付」的 10 个阶段(Idea→Demo→PRD→评审→内审→TRD)线上化,并在每阶段用 AI Agent 辅助起草 / 精修 / 评审 / 分析,解决多文档多系统来回搬运、状态不可靠、并发覆盖的问题,形成一个可跟踪、有权限、可回溯的一站式协作平台。

---

## 核心能力与亮点

### 一、AI Agent 编排与应用开发(重点,投 AI 应用岗)

1. **自研 AI 编排运行时**:基于 Spring AI 封装统一 Agent 运行总线,基于 ReactAgent(StateGraph 条件边)实现 **ReAct 式工具调用循环**(推理→并行多工具→观察→再推理);起草/精修/分析由意图路由分流为独立业务分支,编辑类分支由业务层解析 tool call 生成 diff + 人工确认后落库。
2. **结构化输出工程**:用 `chatForObject + AiStructuredOutputConverter` 把大模型输出强制反序列化为强类型 Java 对象,避免模型自由文本污染数据;解析失败捕获后返回空、由调用方回落默认分支(如意图分类兜底 chat)。
3. **工具调用闭环**:定义 5 种结构化文档编辑工具(replace_section/append_section/patch_lines/rewrite_document/reply_only),局部编辑链路挂载 4 种;经 MCP 协议接入代码仓库 wiki 等外部数据源,TRD 生成时确定性调用并注入上下文(直连优先、MCP 兜底)。
4. **上下文预算控制**:`ContextTokenGuard` 两段式截断防线——超预算先对最长 user 消息做零成本中段截断,仍超限则尾部硬截断循环压到达标;另有 400 响应式激进压缩兜底(压到 60% 水位),防止超 LLM token 上限。
5. **RAG 知识检索**:需求关联文档向量化(标题分块+滑窗),纯向量检索 top-k=12 + 应用层 requirementId 过滤兜底,按文档轮询配额防单文档霸榜;检索失败降级关键词打分,注入可溯源。
6. **可观测与路由**:每次模型调用记录 `requestId/模型/prompt版本/耗时/TTFB`;LLM 意图路由(LLM 分类+置信度阈值+兜底 chat)自动选择任务 prompt 与执行分支。
7. **分层记忆系统(L0-L3)**:为 Agent 构建三层记忆——L0 原始证据 / L1 需求级原子记忆 / L3 跨需求用户偏好;干净对照实验(同需求 + 强制新会话 + 修复 PRD 隐藏泄漏路径)证明 **L1 结构化记忆(963 字符)替代 PRD 注入段(4558 字符),token 节省 42%**,核心架构 100% 一致、质量损失仅 10-15%;L3 零模型成本按频次阈值提炼用户偏好(4 个历史需求沉淀 8 条);实测 L1 很全时 L3 注入边际增益为 0,据此确认两者信息正交、界定 L3 的价值场景。

**技能锚点**:Agent 编排 / ReAct / 工具调用(Function Calling)/ 结构化输出 / RAG / Prompt 工程 / 上下文预算 / 分层记忆。

---

### 二、Java 后端工程能力(重点,投 Java 后端)

1. **工作流状态机设计**:把需求生命周期建模为显式状态机(10 阶段),统一 `transition()` 按 action 分派 `confirm/skip/pass/return` 动作;`guardTransition` 四层服务端鉴权(超管/owner/编辑者/评审名单)防前端门禁被绕过;支持**评审回退**(打回重改、保留后续产物)。
2. **并发一致性(乐观锁)**:文档保存走 `UPDATE...WHERE id=? AND version=?` CAS,冲突抛 `RequirementVersionConflictException`;流程流转按幂等语义有意走 last-write-wins(业务前置校验兜底)——这是一个明示的取舍,正反两面都能讲。
3. **数据容错与降级**:MySQL 主存储,异常自动降级到 Redis 缓存兜底;读口带 try-catch 兜底,新表查询失败保留旧字段;"数据读口切换+新表覆盖旧",前端零改动。
4. **分布式/原子逻辑**:用 `req_sequence`+`INSERT IGNORE`+`LAST_INSERT_ID(current_value+1)` 实现**行级原子自增编号**,天然防并发重复; `GlobalExceptionHandler` 统一异常。
5. **多模块工程**:Maven 多模块(`app/client/web/main`)分层,web(Controller/VO)→ app(core)—依赖清晰;统一响应 `Result`+全链路 traceId。

**选 Java 点**:Spring Boot / 状态机 / 乐观锁 / 事务 / 多数据源降级 / 权限隔离 / 多模块分层。

---

### 三、前端与会话体系(投全栈加分)

- React 18 + TS + Vite + Tailwind;需求列表**无限滚动分页**、详情态乐观更新 + 冲突回滚、投票轮询(耗时防抖)。
- SSO 登录态管理、按角色的侧边导航收起策略;页面细节分页/权限提示。

---

## 技术栈与工具

- **后端**:Spring Boot/Spring AI、乐观锁事务、Redis 缓存、多模块 Maven
- **AI**:Agent 编排、ReAct、工具调用、结构化输出、RAG、Prompt 工程、上下文预算、分层记忆系统
- **前端**:React 18 / TS / Vite / Tailwind、SSO
 
---

## 项目亮点一句话
> 用 **Java 后端 + Spring AI Agent** 构建了一个从 0 到 1 的**全链路 AI 化需求协作平台**——既有扎实的**后端工程**(状态机、乐观锁、容错降级),又落地了一线的 **AI 工程**(Agent 编排、工具调用、结构化输出、RAG、分层记忆)。成功解决了多 Agent/多任务并发写同一数据、文档生成稳定通用、上下文超限等生产级难题。

---

## 其他
- 沟通、文档撰写、代码注释与工程规范意识强;能独立完成需求理解->设计->实现->自测。
- 持续关注 AI/LLM 工程发展。

---

*提交前请:1 替换姓名/联系方式/院校占位;2 依据实际熟练度微调「掌握程度」;3 若有多段实习/项目可并入。*