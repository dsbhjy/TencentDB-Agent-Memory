# 个人简历(秋招)

> **求职意向**:AI 应用开发 / Java 后端
> **说明**:姓名、联系方式、院校占位,提交前替换为真实信息。

---

## 【基本信息】(提交前填写占位:姓名 · 手机 · 邮箱 · 院校 · 学历 · 毕业届 · 意向城市)

---

## 教育背景

**[院校占位]** · 本科/硕士 · **[毕业届]届毕业** · 主修/专业:[计算机科学与技术 / 软件工程]

- 相关课程:Java 程序设计 / 数据库 / 操作系统 / 计算机网络

---

## 项目经历

### 一站式 AI 需求协作平台(Web + 桌面端,主导后端核心)

**技术栈**:Java 17 / Spring Boot / Spring AI / React 18 / TypeScript / Vite / MySQL + Redis(JIM)+ Electron

**一句话**:把「需求从创意到交付」的 10 个阶段线上化,并在每阶段内嵌 AI Agent 辅助起草/精修/评审,解决多文档搬运、状态不可靠、并发覆盖三类问题,做成一条**可跟踪、有权限、可回溯、AI 长在流程里**的需求流水线。

---

## 核心亮点

### 一、AI Agent 端到端链路(投 AI 应用岗)

完整链路:用户输入 → 意图路由 → 知识检索 → 结构化生成 → 结果校验 → 失败兜底。

1. **意图路由**:进生成/chat 前置判断「生成文档 / 编辑文档 / 纯聊天」,选任务 prompt 与执行分支,杜绝「随口聊一句」误触整篇重写(细节见「优化动作」)。
2. **知识检索(RAG)**:需求关联文档按标题分块向量化,针对 **Vearch 向量库不支持 filter 下推**的坑,改成纯向量检索 + 应用层 requirementId 过滤 + 按文档轮询配额防单文档霸榜;检索失败降级关键词打分。
3. **结构化生成**:基于 ReactAgent(StateGraph 条件边)实现 ReAct 式工具调用循环,`chatForObject` 强类型反序列化,编辑动作建模成 5 种结构化工具,真实执行由业务层解析 diff + 人工确认后才落库——**模型不直接改数据**。
4. **结果校验**:编辑类输出走「diff + 人工确认」,模型幻觉的风险兜在「人」这一层。
5. **失败兜底**:`ContextTokenGuard` 上下文预算 + 解析失败回落 + 检索失败降级,各环有对应兜底(细节见下)。

**能评估**:分层记忆系统的对照实验(见下)是本项目落地的一手量化证据——token 省 42%、质量损失 10-15%,盲评 + 关键词覆盖 + 污染监测三重判据。

---

### 二、分层记忆系统(L0-L3)(深挖亮点)

Al 生成 TRD 时靠整段注入历史 PRD,单次 user token 达 **14211**;且跨需求稳定偏好零记忆。设计三层:

- **L0 原始证据 → L1 需求级原子记忆(决策/约束/事实/待确认,按需求隔离)→ L3 跨需求用户偏好(零模型成本,词表+频次阈值提炼)**。
- **注入顺序 L1 最顶、双层 fail-open**,记忆系统坏掉不影响主生成链路。

**对照实验(可出示设计文档)**:同需求 + 强制新会话 + 修复 PRD 隐藏泄漏路径,得出 **L1(963 字符)替代 PRD 注入段(4558 字符),token 14211→8246 省 42%**,产物体积几乎相同、核心架构 100% 一致、质量损失 10-15%;并实测「L1 很全时 L3 边际增益为 0」,据此界定 L3 价值场景在 L1 薄/空的新需求初期——两条信息正交,不无脑叠加。

---

### 三、AI 工程小功能(优化动作,每条 = 一个具体取舍)

1. **意图路由 `IdeaIntentRouter`**:用户只说「就按你说的改」这类指代意图时,模型容易判错。方案是 LLM 分类 + 置信度阈值(低于阈值兜底 chat)+ 解析失败兜底 + 30s 硬超时,**三层兜底保证判不准时宁可当聊天、不误触重写**。
2. **上下文预算 `ContextTokenGuard`**:`prd/trd` 动辄几千字会把 token 撑爆。方案是**两段式截断**——超预算先对最长 user 消息做零成本中段省略,仍超限则尾部硬截断循环压到达标,再加 400 响应式激进压缩兜底(压到 60% 水位),逐段降级而不是一刀切。
3. **结构化输出 `chatForObject`**:大模型自由文本会污染下游数据结构。方案是强制反序列化为强类型 Java 对象 + 容错解析(JSON 截断抢救、裸引号修复),解析失败捕获后回落默认分支——**格式约束写进 prompt,失败不中断主链路**。

---

### 四、Java 后端工程(投 Java 后端)

1. **服务端状态机**:需求 10 阶段建模为显式状态机,统一 `transition()` 按 action 分派;`guardTransition` 四层服务端鉴权(超管/OWNER/编辑者/评审名单)防前端门禁绕过;支持**评审打回重改且保留后续产物**。
2. **并发一致性**:文档保存 + AI 回写走同一条 `version` CAS(乐观锁),冲突显式化;**冲突按来源分级解决——状态类冲突(投票/流转)静默重试,正文类冲突按 PRD 章节三方合并(改不同章节自动合、同章冲突弹框对比裁决)**,多人与 AI 并发编辑互不覆盖;流转按幂等语义有意走 last-write-wins + 业务前置校验兜底。
3. **数据容错**:MySQL 主存储 + Redis(JIM) 自动降级兜底;新表查询失败保留旧字段,前端零改动。
4. **原子编号**:`req_sequence` + `INSERT IGNORE` + `LAST_INSERT_ID(current_value+1)` 行级原子自增,并发创建不重号。
5. **工程规范**:Maven 多模块分层(main→web→app→client)、统一 `Result` + 全链路 traceId。

---

## 技术栈

- **后端**:Spring Boot / Spring AI、状态机、乐观锁、Redis 降级、多模块 Maven
- **AI**:Agent 编排、ReAct、工具调用、结构化输出、RAG、上下文预算、分层记忆
- **前端**:React 18 / TS / Vite / Tailwind、SSO

---

## 项目亮点一句话

> 用 Java 后端 + Spring AI Agent 从 0 到 1 构建**全链路 AI 化需求协作平台**:后端有扎实的**状态机、乐观锁、容错降级**,AI 侧落地了**端到端 Agent 链路 + 分层记忆**,并用对照实验量化了「上下文成本降 42%」。核心不是「做个 AI 工具」,而是把研发流程固化成系统、再把 AI 长进系统的固定环节里。

---

## 其他

- 能独立完成需求理解 → 设计 → 实现 → 自测;代码注释与工程规范意识强。
- 持续关注 AI/LLM 工程发展。

---

*提交前:1 替换占位信息;2 依据熟练度微调掌握程度;3 深挖细节与取舍话术见《面试详述稿.md》。*