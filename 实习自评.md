# 实习自评 · 云枢项目

## 一、实习概况

参与云枢建设，把需求从创意到交付散落在文档、IM、表格里的人工协作，收拢成结构化需求流水线，关键节点内嵌 AI Agent 代替人工起草/精修/评审。

技术栈：Java 17 · Spring Boot · Spring AI · MySQL + Redis · React 18 / TS / Vite / Electron。

## 二、结果达成

**1. 解决多人 / 多线并发覆盖的隐蔽 Bug**
问题：需求会被多人同时编辑，AI 多线工具也并发回写，无保护则后写覆盖。
结果：引入版本号乐观锁，事务内按主键 + 版本号更新，版本不一致即抛显式异常，前端以最新为准重新加载；需求编号用独立序列表原子自增。把丢数据变成显式冲突、可恢复。

**2. 把评论 / 投票从需求快照 JSON 拆到独立评论表**
问题：评论、投票、回复都塞在快照 payload JSON 里，随需求整体读写、快照膨胀。
结果：拆到独立评论表，写只写新表、读用新表覆盖旧字段，前端零改动；存量数据用迁移程序分页补入，单条失败只回滚该需求，迁移期新旧并存、对前端无感。

**3. 构建 AI 预审：让 PRD/TRD 评审前先过一道 AI 审阅**
问题：PRD/TRD 评审前缺少前置把关，遗漏要等到评审会上才发现。
结果：逐章节调用大模型做结构化审阅，输出带维度、问题、证据、建议、严重度的建议；RAG 召回比对一致性与遗漏；建议落入暂存箱，可采纳或忽略，把评审从找低级问题解放到讨论关键决策。

**4. 构建评论回复逻辑与企业IM 通知**
问题：评论只有平铺一层，没法追问；提交后参与者无从及时知晓。
结果：搭起回复父子链路，新增回复挂到父节点下，按父子组装成树；评论保存后按角色从成员表裁决人群，去重排除操作人，推送带跳转的企业IM卡片。评论升级为可追问的讨论树。

**5. 为 AI Agent 构建分层记忆系统（L0-L3）**
问题：生成 PRD/TRD 靠整段注入历史 PRD 当背景（单次 4558 字符、user token 14211），成本高；AI 对用户跨需求的稳定偏好零记忆，每次从零开始。
结果：设计三层记忆——L0 原始证据、L1 需求级原子记忆（按 requirementId 隔离）、L3 跨需求用户偏好；L3 用关键词匹配 + 频次阈值零模型成本提炼（4 个历史需求沉淀 8 条偏好），注入生成链路且 fail-open。干净对照实验（同需求、强制新会话、修复 context JSON 隐藏泄漏 PRD 的路径）验证：L1 结构化记忆（963 字符）替代 PRD 注入段，token 节省 42%，核心架构 100% 一致、质量损失仅 10-15%；并实测 L1 很全时 L3 注入边际增益为 0，据此确认两者信息正交、把 L3 价值场景定位在 L1 薄/空的新需求初期。

## 三、成长收获

- **工程思维**：从能跑就行转向先想清边界再写代码；多步功能先定顺序、不遗漏。
- **AI 工程落地**：通过 AI 预审与分层记忆系统，学到结构化输出、RAG 召回、暂存箱采纳/忽略、软降级、fail-open 注入等工程兜底。
- **对照实验方法学**：记忆系统验证中学会「先怀疑结论」——指纹日志确认注入真实生效、强制新会话排除会话记忆污染、排查出 context JSON 隐藏泄漏 PRD 的路径（此前所有「关 PRD」实验并未真正关掉）、以盲评 + 关键词覆盖 + 污染监测三重判据评估质量。
- **全链路视角**：从后端并发、拆表到前端冲突回滚，走通需求 → 设计 → 实现 → 自测。
- **不足与后续**：缺少与同事、领导沟通和向上负责的意识。
