# 上下文管理机制(分层记忆 + 整体防线)

> 本文档把「上下文到底是怎么管理的」讲透,区分两个常被混为一谈的层面:
> **① 分层记忆(L0/L1/L3)解决「注入什么」——每层的取舍;② 上下文整体管理机制解决「装得下吗」——注入层、会话层、预算防线、模板层的四层配合。**
> 代码证据锚:全部在 `jkc-copolit-engine-app/src/main/java/com/jd/jkc/vehicle/jkc/copolit/engine/app/agent/runtime/`,核心是 `AgentPromptComposer.java` 与 `ContextTokenGuard.java`。

---

## 零.先把「分层取舍」和「截断取舍」掰开

面试时最常见的错误是把「L1/L3 怎么取舍」和「两段式截断」混为一谈。它们是**不同层的问题**:

| 层面 | 回答的问题 | 属于哪块 |
|---|---|---|
| **分层记忆取舍** | 注入什么、每层给哪些内容、优先级 | 显式注入层(`AgentPromptComposer`) |
| **上下文截断** | 装了这些内容**放不下怎么办** | 预算防线(`ContextTokenGuard`) |

「分层取舍」是**内容选择**(该给模型哪些信息);「截断」是**容量兜底**(超载时怎么压缩)。两者独立存在、独立开关、互不替代。

---

## 一、分层记忆:每层如何取舍

记忆注入集中在 `AgentPromptComposer.buildTrdArtifactUserPrompt()`(L186-247)。每一层的选择性(取舍)机制不同:

### 1. L1(需求级原子记忆)——「全量 + 强指令」

**取舍策略**:按 `requirementId` 隔离,取该需求**所有 active atoms**(上限 30),**不设阈值、不做覆盖率裁剪**——因为 L1 是「本需求的事实」,要求全量承接,不能只给一部分。

**分组结构**(`MemoryL1RecallService.renderRequirementMemory`, L48-64):
按 `memory_type` 渲染成六个小节,每节一个 `###` 标题:

```
### 已定决策   (decision)
### 约束       (constraint)
### 待确认     (open_question)
### 风险       (risk)
### 事实       (fact)
### 任务提示   (task_hint)
```

**强指令机制**(这是对抗「L1 丢失数值精度」的直接对策,源自 §7.10 实验发现):
```text
> 以下是本需求已确认的决策、约束与待确认项,必须在技术方案中全部体现,不得遗漏。
> 其中涉及的具体数值、时间窗口、口径、公式等必须逐字保留,不得改写或泛化。
```
文末再加一句:「上述每一项都必须在方案正文中明确响应;若某项不适用,需显式说明原因,不得静默忽略。」

**顺序**:L1 **最后 prepend,位于 prompt 最前**(最高优先级)。即 `L1 → L3 → 基础上下文`。

**fail-open**:仓库异常时 catch 返回空串(日志 `L1 recall skipped`),不影响主链路。

### 2. L3(跨需求偏好)——「只给已确认 + 参考级降级」

**取舍**(`MemoryL3RecallService.renderUserPreference`):
- **只注入 `auto_confirmed / manual_confirmed` 的偏好;`candidate`(低置信)不注入**——`repository.findInjectablePreferences` 只返回已确认,避免低置信模式污染生成。
- **上限 10 条**。
- **优先级别定为「参考级」**:注入的 markdown 明确写——「以下偏好来自该用户多个历史需求的稳定模式,供生成方案时参考,**优先级低于当前需求的具体决策与约束**」。

**与 L1 共同注入时的顺序**(Composer L201 注释明确标注):
```
L1(本需求事实,最高优先级,最前) → L3(跨需求偏好,参考级)
```
L3 先 prepend,再 L1 prepend,最终 L1 在最前。

### 3. L0(原始证据)——「不注入,只做底座」

注入链路(Composer)中**没有任何 L0 被读进 prompt**:
- L0 是**证据底座**:`MemoryL0CaptureService` 在每轮 Agent 对话完成时落库原始证据;
- 用途是**审计追溯 + L1 提炼的原料**(`MemoryL1ConsumerService` 扫 pending L0 → LLM 抽 L1 atoms);
- **L0 不直接进 trd/prd 的 prompt**,所以它**不存在「截断取舍」**——它是上游原料,不是注入内容。

> 面试要点:「L0 不是给模型看的记忆,是给系统兜底的一套证据仓库——它决定 L1 从哪来、以及每一条记忆能不能追溯到原文。」

---

## 二、上下文整体管理机制(四层配合)

上下文不是「一堆记忆」,而是由**四个相互独立、各司其职的机制**组合而成。分层的记忆只是其中一层。

### 机制① 显式注入层(Composer 组装)——内容「放什么」

TRD user prompt 的最终组装顺序(`AgentPromptComposer.buildTrdArtifactUserPrompt`):

```
L1(本需求记忆,最前,高优先级)
  → L3(跨需求偏好,参考级)
  → 基础上下文(IDEA / context JSON / 用户输入)   ← AiContextBuilder
  → PRD 段(可选,受 trdPrdInjectEnabled 控制)
  → 参考文档段(trd 参考 doc)
  → 架构段(architectureContext,受 agent.trd.artifact.architecture 模板)
```

每块都是**独立开关 + 独立 fail-open**:
```
memory.l1.trd-inject.enabled     ← L1
memory.l3.trd-inject.enabled     ← L3
memory.l1.trd-prd-inject.enabled ← PRD 段
```
实验的 42% / 10-15% / 剥离 PRD,就是在这一层做变量控制(切开关、剥离 context 的 prd)。

### 机制② 会话记忆(advisor 运行时注入)——按 session 隔离

- 运行时由 Spring AI advisor 注入**当轮对话历史**(`agent:trd:session:XXX`,JDBC 持久化)。
- 按 `session_id` 隔离,`count` 记录已写入的 user+assistant 轮次。
- **与分层持久知识物理分开**:分层记忆是「持久知识库」,会话记忆是「当轮上下文」。两者是不同机制,不互相覆盖。
- 实验强制随机新 session 就是为了让此层 `count=0`(无历史污染)。

### 机制③ 预算防线(ContextTokenGuard)——「包装得下吗」

`ContextTokenGuard` 在**请求发出前**按模型窗口统一判定,超预算才触发渐进式压缩。这是「截断」所在层——**兜底,不是常态**:

```
windowTokens    = windowTokensFor(modelKey)          // 按模型查表
availableBudget = windowTokens - maxTokens           // 输出预算扣除
                - toolsBudget - memoryReserve         // 工具描述 + memory 预留
guardThreshold  = availableBudget * 0.87              // 87% 触发压缩
promptTokens    = Σ estimateTokens(message)
overLimit       = promptTokens > guardThreshold       // 超限才压缩
```

**渐进式三层压缩(参考 Claude Code Auto-Compact):**

| 层 | 名称 | 动作 | 是否启用 |
|---|---|---|---|
| Layer 1 | Micro-Compact(零成本) | 对**最长 user 消息** `trimMiddle`(保留首尾各一半,中段省略) | ✅ |
| Layer 2 | Summarize-Compact(LLM 摘要) | 按优先级逐段摘要(参考文档→wiki→PRD→memory 历史) | ❌ 预留接口,因递归调用风险**未启用** |
| Layer 3 | Hard-Trim(兜底) | 从最长 user 消息**尾部硬截断**循环 | ✅ 兜底 |

**两条触发路径**:
- **预防性 guard(`guard()`)**:`promptTokens > 87% 阈值` → 走 Layer1 → 不够走 Layer3。
- **400 回流 guard(`guardAggressive()`)**:网关已返回 400(预防性 guard 因 token 估算偏差未拦住)→ **跳过 Layer1,直接 Layer3**,压到可用预算 60% 更激进水位,确保重试不超限。

**注意**:此层压缩的是**整个 prompt**(system+user 全部消息),记忆只是其中一段,被当成普通消息一起压缩——**它不是「只裁记忆」,也不做「记忆哪段保留」的选择,那是机制① 的事。**

---

### 机制③ 补充:token 预算的全貌——不只是一个阈值

「token 预算」不是一个单值,而是一套**分块窗口预算**体系。除上面 ContextTokenGuard 的 87% 请求预算外,还有三套独立预算,分布在 `ContextTokenConfig`:

#### (a) 请求预算(ContextTokenGuard,已在机制③)

- 分母是 `windowTokensFor(modelKey)`——**按模型查窗口**,不是固定值。内置表:
  | 模型 | 窗口 token |
  |---|---|
  | GLM-5.1 | 200K |
  | GLM-5.2 | 1M |
  | DeepSeek-V4-Pro | 1M |
  | GPT-5.5 | 1.05M |
  可 DUCC 覆盖/新增;未配置 fallback 200K。分母下的每一类内容都是窗口的占比。

- 预留三块: `maxTokens`(输出)、`toolsBudgetTokens`(约 4000)、`memoryReserve = window × 0.3`(会话历史)— 三块不在 prompt 内,所以算「给 prompt 留的容量」先扣除。
- **87% 触发线**:字符估算对 JSON/代码低估约 20%,0.87≈1/1.15 兜住预热期。

#### (b) 会话历史滚动摘要(BudgetedChatMemoryAdvisor)——memory 那套

针对 `agent:trd:session:XXX` 会话历史,是另一套独立的按 token 占比触发:

```
softThreshold = window × 0.7   历史 token 超 70% 窗口 → 触发滚动摘要
retainTokens  = window × 0.3   摘要后保留最近 30% 原文
hardLimit     = window × 0.5   拼接后仍超 → 从最老消息硬丢弃(防网关 400)
```

这与 ContextTokenGuard 物理分开:前者管「整条请求的组装预算」,后者专管「会话历史 memory」的读写长度。

#### (c) 各注入块的独立预算(细分到块,超限逐段语义摘要)

给每个显式注入块单独配窗口占比阈值,各自超限各自压缩(不是一刀切删):

| 块 | 预算占比默认 | 超限处理 |
|---|---|---|
| 参考文档(JoySpace) | window × 0.25 | 逐仓库语义摘要 |
| 架构 wiki | window × 0.15 | 多仓逐仓库摘要 |
| PRD 全文 | window × 0.15 | 语义摘要压缩 |
| 用户本轮输入 | window × 0.5 | 入口拦截,提示精简/拆分 |

#### (d) token 估算基础(`estimateTokens`)

轻量近似,不求精确对账:
```
中文:1 token/字;英文等其他:1 token/ 4 字符
```
只用于「是否触发压缩」的快判;精确 usage 由网关返回后日志侧记录。**对 JSON/代码低估约 20%** — 这是 87% 余量与 400 逃生道存在的根因。

**一句话串起来**:四套预算组成的目的是「把一个 token 当资源管理」——按模型窗口算总容量、预留输出/工具/历史三块、分块给每个注入段配独立占比阈值、超限各自降级(滚动摘要/语义摘要/硬裁剪)、估算低估留 87% 余量、万一还是 400 用 60% 激进重试兜底。**工程价值点是『分块预算 + 阈值 + 各自降级』,不是简单『写不完就删』。**

### 机制④ 模板/RAG/规则层——非记忆但也在上下文里

- `resolveNamedPrompt / renderNamedFallback`:规则片段(如 `agent.mermaid.rule`)、模板、知识检索结果拼进 prompt。
- 这些不在 Prompt 内但叠加的部分,由 `ContextTokenGuard` 的 `toolsBudgetTokens`(约 4000)、`memoryReserveRatio`(0.3)预留预算。
- mermaid 规则等「纯字符串拼接」而非模板占位,避免字面 `{ }` 与 StringTemplate 的解析冲突。

---

## 三、四层关系一张图

```
用户请求
  │
  ├─① 显式注入(Composer): L1(最前,高优先级) → L3(参考级) → 基础上下文
  │        → PRD段 → 参考文档 → 架构;每块可开关 + fail-open
  │       ▲ 分层记忆(取舍:注入什么 / 每层多少 / 只给哪些)
  │
  ├─② 会话历史(advisor): 按 sessionId 隔离,和分层知识物理分开
  │
  └─③ 预算防线(ContextTokenGuard 请求前): 超87%阈值 → Layer1 中段省略
          → Layer2(预留未启) → Layer3 尾部硬截断 → 400后 60% 激进重试
       ▲ 截断(取舍:装不装得下 / 超载如何强制)
```

---

## 四、一经串起来的话术(面试 30 秒版)

> 「上下文管理分**两个层面**:一是**显式注入层**——分层记忆在这里做内容取舍,L1 全量+强指令、L3 只给已确认偏好且标参考级、L0 不注入只当证据底座,再加 PRD/参考文档/架构块,每块独立开关和 fail-open;**二是**容量防线**——Composer 之外还有一个 token 预算 Guard,超负载才触发渐进式压缩(层1 中段省略、层2 摘要预留未启、层3 尾部硬截断兜底)。会话历史按 session 隔离是第三个独立机制。三块配合:注入层决定'放什么',会话层决定'这段对话',预算层决定'装得下吗'。」

---

## 五、代码锚点清单

| 关注点 | 文件 | 位置 |
|---|---|---|
| 注入组装入口 / 顺序 | `agent/runtime/AgentPromptComposer.java` | `buildTrdArtifactUserPrompt` L186-247 |
| L1 recall(分组 + 强指令) | `agent/runtime/memory/l1/MemoryL1RecallService.java` | `renderRequirementMemory` L34-66 |
| L3 recall(只给已确认) | `agent/runtime/memory/l3/MemoryL3RecallService.java` | `renderUserPreference` L33-54 |
| L0 底座 / capture | `agent/runtime/memory/l0/MemoryL0CaptureService.java` | — |
| L1 消费(从 L0 抽) | `agent/runtime/memory/l1/MemoryL1ConsumerService.java` | — |
| 预算 / 三层压缩 / 400 兜底 | `agent/runtime/ContextTokenGuard.java` | guard L94, guard_400 L149, Layer1 L230, Layer3 L282-L339 |
| 预算配置 | `agent/runtime/memory/ContextTokenConfig.java` | window/code 映射 |

---

*本文档与《分层记忆实验方法学.md》互补:前者讲「数据怎么测得准」,本文讲「这套系统在工程上怎么组成」;面试两条线都过一遍即可。*