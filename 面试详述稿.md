# 面试详述稿 — 两条深挖亮点 + 全链路防守卡

> 用途:面试/答辩被深挖时翻。分三部分——①端到端链路(证明不是拼装 demo)②两条深挖亮点(分层记忆 + Agent 运行时,每条能口头讲 10 分钟)③防守卡(所有会被追问、且核实过代码的取舍话术)。
> 所有事实均已代码级核实(见《简历-待调查清单.md》33/33 核实结果),面试话术以「核实版本」为准,不要复述旧简历或被证伪的表述。

---

## 一、端到端链路(先讲这个,证明踩中了审查二第二条)

一句话把六环串起来,对应审查二的标准链路「用户输入 → 任务识别 → 信息获取 → 结果生成 → 结果校验 → 失败兜底」:

> 用户在需求详情里说一句话(输入),先过 `IdeaIntentRouter` 判断是要生成 / 编辑文档 / 还是纯聊天(任务识别);要生成时 `RAG` 把需求关联文档向量检索出来做知识底座(信息获取);`Agent 运行时` 里的模型据此产出,并用 `chatForObject` 强类型反序列化成结构化对象(结果生成);编辑类动作经业务层解析成 diff 交人工确认后才落库(结果校验);模型抽风、超 token、解析失败、检索失败各自有兜底——意图兜底 chat、两段式截断、Optional.empty 回落默认、关键词打分降级(失败兜底)。

每环都能对应到具体模块与代码,面试官问「链路在哪」时逐环指:

| 链路环节 | 模块 | 关键取舍(一句话) |
|---|---|---|
| 用户输入 | 需求详情对话框 | — |
| 任务识别 | `IdeaIntentRouter` | LLM 分类 + 置信度阈值 + 三层兜底 chat + 30s 硬超时 |
| 信息获取 | `SpringAiRagFacade` | Vearch 不支持 filter 下推 → 纯向量 + 应用层 requirementId 过滤 + 防霸榜 |
| 结果生成 | `SpringAiAgentRuntime` + `chatForObject` | ReactAgent(StateGraph 条件边),model↔tool 循环;结构化输出避免自由文本污染数据 |
| 结果校验 | 业务层解析 tool call → diff → 人工确认 | 工具 Function 是占位,不直接让模型改数据 |
| 失败兜底 | `ContextTokenGuard` / 降级链 | 两段式截断 + 检索关键词兜底 + 解析 Optional.empty 回落默认 |

---

## 二、深挖亮点 A —— 分层记忆系统(L0-L3)

### 2.1 痛点(先讲「问题是什么」)

AI 生成 TRD 时,把历史 PRD 整段塞进 prompt 当背景,单次 user token 到 **14211**;而且每个新需求都要用户重复交代「我要用 Java17 / 文档要这种风格 / 敏感信息要脱敏」这类稳定偏好——上下文越来越贵,AI 却记不住用户。

### 2.2 设计(「为什么三层」,不是拍脑袋)

三层各司其职,信息正交:

- **L0 原始证据**:对话落库,不经加工,是可审计的事实来源。
- **L1 需求级原子记忆**:从 L0 抽取「决策/约束/事实/待确认」四类原子,按 requirementId 隔离。一个需求的事实只服务于这个需求,**不会污染别的需求**。
- **L3 跨需求用户偏好**:从多个需求的 L1 里提炼「跨需求反复出现的模式」(技术选型、文档风格),**零模型成本**——不调用 LLM,纯关键词 + 频次阈值。

关键设计点(面试官会问「为什么分三层」):**L1 是「这个需求的事实」,L3 是「这个用户的模式」,两者信息正交**。我用对照实验实证了这一点——L1 很全时,L3 注入的边际增益是 0。这就把 L3 的价值场景精准界定在「L1 薄/空的新需求初期」,而不是无脑叠加。

### 2.3 验证方式(审查二第三条:怎么评估)

分三层留痕,面试可出示:

- **指纹日志**:`AgentPromptComposer` 打印 "TRD L1/L3 inject fingerprint"(requirementId / injected / memoryLen / hasMarker),每次注入可追溯。
- **prompt 快照**:`AgentTrdArtifactStreamService` 记录 "TRD artifact prompt size"(systemChars / userChars / userTokens),token 变化可对比。
- **设计文档**:`docs/superpowers/specs/2026-09-02-memory-l1-design.md` §7.9 / §7.10 / §7.11,完整对照实验数据。

### 2.4 核心数据(面试口径,只引用最可信那一轮)

**统一引用 §7.10(230 彻底剥离轮)**,而不是更早的 §7.8(有 context JSON 泄漏 PRD 嫌疑):

- token **14211 → 8246,节省 42%**
- 产物体积几乎相同,核心架构 100% 一致,质量损失 10-15%(集中在数值精度)
- 五维均分 3.7 vs 3.9(质量略有损失换取 42% token)
- 三重判据:盲评 + 关键词覆盖 + 污染监测;对照组做了「强制新会话」排除会话记忆污染

### 2.5 「为什么这么设计」的三个真实取舍(含金量所在)

1. **L1 用「结构化原子」而非「原文检索」**:原文检索会带回 token 和噪声;原子化后以最小信息量保留「发生过什么决策」,并且能按类型(决策/约束/事实)分类取用。
2. **L3 不调模型,用词表+频次**:调模型成本高且结果不稳;用硬编码的 10 主题词表(降级/幂等/脱敏/可用性/状态流转/灰度/重试/异常边界/分模块/监控告警,每主题 4 关键词)+ candidate≥2 / auto_confirmed≥3 阈值,按「覆盖需求数」去重计频(同一需求多条 atom 只算 1 次,防单需求虚增),零成本且可解释。
3. **注入顺序 L1 最顶、双层 fail-open**:实验证实「开头比末尾好」(末尾会被 context JSON 淹没);fail-open 保证记忆系统坏掉时不影响主生成链路(Recall 层 catch 返回空串 + Composer 层 catch 跳过注入)。

---

## 三、深挖亮点 B —— Agent 编排运行时

### 3.1 痛点

直接把 LLM 接对话有三个坑(面试要能一口气说出来):

1. 模型只能「回答」不能「真的改文档」——说一堆正确建议但落不了地;
2. 长文档(PRD/TRD 几千字)会把 token 上下文撑爆;
3. 「帮我写 PRD」「这段改一下」「随便聊聊」走同一条路,分不清该生成/编辑/聊天。

### 3.2 三条应对(每条都有「为什么」)

1. **会调工具的 Agent**:ReactAgent(StateGraph 条件边)实现 model↔tool 循环,模型不再产生 tool call 即退出;`DocEditTools` 把编辑动作建模成 5 种结构化工具。**关键取舍:工具 Function 是占位,真实执行在业务层解析 tool call → diff → 人工确认**,杜绝模型自由文本直接改数据。
2. **上下文预算 `ContextTokenGuard`**:两段式截断——超预算先对最长 user 消息零成本中段截断,仍超限尾部硬截断循环达标;400 响应式激进压缩兜底(60% 水位)。
3. **意图路由 `IdeaIntentRouter`**:进 generate/chat 前判断用户是生成/编辑/聊天,选任务 prompt 与执行分支,避免「随口聊一句」误触整篇重写。

### 3.3 三条小功能逐条深挖(面试被单独问「它到底怎么做的」时用)

**① 意图路由 `IdeaIntentRouter`**

- **痛点**:用户常说「就按你说的改」这类指代,模型只拿到当前单条 message 时判不出,容易把「纯聊天」误判成「整篇重写」,成本翻倍还破坏文档。
- **设计**:LLM 分类(复用请求主模型,非轻量专用模型)+ 置信度阈值(热更可调)+ **三层兜底**(分类器失败→chat、置信度<0.8→chat、confidence 异常→默认 0.75)+ intent 节点禁重试 + 30s 硬超时防 hang。
- **取舍**:判不准时**宁可当聊天、绝不误触重写**——误重写的代价远大于一次错误归类为聊天的代价。

**② 上下文预算 `ContextTokenGuard`**

- **痛点**:PRD/TRD 动辄几千字,整段进 prompt 会撑爆 token 上限,还费钱。
- **设计**:两段式截断(vs 追求语义分层)——超预算(87% 水位)先对最长 user 消息做**零成本中段省略**(保留首尾各半 + 省略标记),仍超限则**尾部硬截断循环**压到达标;另有 400 响应式激进压缩兜底(60% 水位)。
- **取舍**:抽「零成本字符级截断」而非「LLM 摘要」——摘要层是预留接口,因会递归调网关有风险而未启用(下一步计划)。这是「先上便宜可靠的,复杂方案留作演进」的工程选择。

**③ 结构化输出 `chatForObject`**

- **痛点**:大模型自由文本会污染下游数据结构(需求字段、投票、评论),直接入库 = 脏数据。
- **设计**:`AiStructuredOutputConverter` 把输出强制反序列化为强类型 Java 对象;容错解析做 **JSON 截断抢救 + 裸引号修复**;格式约束经 `BeanOutputConverter.getFormat()` 拼进 prompt,让模型知道该输出什么形状。
- **取舍**:解析失败**捕获后返回 Optional.empty、不重试不抛异常**,由调用方回落默认分支(意图→chat、评审→本地启发式);需硬失败场景另有 `chatForRequiredObject` 显式抛异常。失败不中断主链路,是明确的降级语义。

### 3.4 「为什么这么设计」的取舍

- **工具分层**:5 种工具里 `rewrite_document`(整篇重写)在主链路**未启用**——因为把整篇正文塞进工具参数会导致长时间无流式输出,用户体验差。这是「设计时主动放弃一个能力换体验」的取舍。
- **编辑执行「人工确认」**:模型产出 diff 不直接落库,让人确认——这是把「AI 会幻觉」的风险兜在「人」这一层,而不是赌模型不出错。
- **MCP 接入边界**:外部数据源(代码仓库 wiki)经 MCP 协议接入,但**是业务代码确定性调用、作为上下文注入,不是模型自主工具调用**——避免 Agent 裸跑 MCP 的危险操作面。

---

## 四、防守卡(核实过代码、面试会被追问的点)

> 每条 = 「问题是什么 → 真实答案 → 怎么说才既诚实又不减分」。按被追问概率排序。

### 4.1 Agent 运行时(最易穿帮,先背)

1. **别讲「最大轮数 maxIterations」**:字节码证明该框架版本未消费此参数(recursionLimit=MAX_VALUE)。被问就答:「停止条件是模型不再产生 tool call,业务层一次 call 封顶」。
2. **别讲「逐级压缩(先历史后文档)」**:真实是两段式截断(trimMiddle 中段省略 → 尾部硬截断),不是语义分层;「摘要层」是预留接口未启用。被问摘要就答「预留了接口,因递归调用网关的风险未启用,是下一步计划」。
3. **别讲「Agent 统一接入工具网关」**:网关是业务代码确定性调用,**Agent 工具集全是本地的**(DocEditTools/Skills/KnowledgeQA),网关在 Agent 链路零调用。被问就答「外部数据源经 MCP 接入作为上下文确定性注入,不进模型自主工具调用」。
4. **意图路由不带历史**:`IdeaIntentRouter` 只传当前单条 message,「就按你说的改」这种指代判不出,会兜底 chat——这是已知边界,诚实讲即可。
5. **结构化输出容错解析的边界**:JSON 截断抢救/裸引号修复只能抢救「局部格式坏」,抢不回「语义错」;解析失败返回 Optional.empty 不重试、由调用方回落默认分支。被问「脏数据怎么办」就答「格式约束写进 prompt 前置防错 + 解析失败不落库、走降级,硬失败场景有 chatForRequiredObject 显式抛异常」。
6. **上下文预算「为什么不用摘要」**:摘要层(LLM 摘要压缩)是预留接口,因会递归调网关有风险而未启用。被问摘要就答「先上零成本字符级截断保可用,摘要作下一步演进——递归调用网关可能死循环,这是主动不上的取舍」。

### 4.2 状态机(分量最重,四条都站得住)

1. **「加一个阶段改几处」要精确**:后端 6 处(STAGE_ORDER / switch case / allowedMembersForAction / computedStatus / NodeRollbackService 副本 / WorkbenchOverviewService 副本,另有 returnForModify 逐 key 硬编码)、前端至少 4 处、同一 10 阶段列表前后端共 4 份副本。**主动报出「改 10 处」这个数字是加分谈资**(诚实 + 全局视野)。
2. **四层鉴权**:超管(DUCC 白名单)/ OWNER / EDITOR / 评审名单(快照 payload JSON 内),权限带 Redis 300s 缓存。
3. **`returnForModify` 清/留语义**:清编辑锁、确认跳过标记、版本徽标;保留投票、评论、PRD/TRD 文档、任务(stageStatus 置 pending)。区分「评审打回(留数据)」vs「超管后门项目软删」两套语义,别混。
4. **非法流转三类错误码**:400000(不支持动作)/ 500001(业务前置不满足)/ 403000(越权带 ownerErp)。**注意:不存在「链路已完成重复提交」和「当前节点不对」的异常**——这是旧简历说法,已被证伪。

### 4.3 并发(诚实讲取舍,这是最大的加分点,也是最不能吹的)

1. **乐观锁只护「手动保存 + AI 回写」,自动保存路径(last-write-wins)是被显式放弃的**。前端注释自认「避免 AI 异步回写/投票使 version 过期导致 409 打断连续输入」。
2. **最高频写路径(自动保存,2s 防抖)完全无锁**,A/B 同时开着编辑器会各自每 2 秒整篇覆盖,后写抹掉先写。
3. **冲突后「直接覆盖用户编辑」是代码事实**:手动撞版本后前端自动重试 = 拿本地全文再覆盖,A 的修改被静默丢弃。
4. **面试口径(必须主动讲取舍,绝不辩解)**:「单人为主 + 自动保存体验优先,这是明示的 trade-off」;改进三件套——冲突 diff 对比合并框 / 章节级 version(拆 PRD 章节数组按 section 乐观锁)/ 本地草稿暂存重放。被问「丢了修改怎么办」→ 先承认没有保护,再给「为什么(成本 vs 场景)+ 改进方向」。

### 4.4 数据容错(细节别讲错)

1. **命名**:代码里是 `saveToJim / getFromJim`(JimDB,Redis 兼容),简历写「Redis」是脱敏。面试讲代码用 JimDB,主动说明即可,不算破绽。
2. **别写「降级保证一致性/新鲜度」**:降级口 catch RuntimeException 全量降级,恢复后两条存储各自演化无对账。但**有三处显式不降级的强一致通道**(getStrict / patchFieldsStrict),这是加分点,可以主动讲。

### 4.5 数字口径(有实据的才说,零证据的一律不造)

| 可放心写(代码/文档有据) | 绝不写(零证据/已证伪) |
|---|---|
| 分层记忆 token 14211→8246 省 42%(§7.10) | 工具调用成功率(无测试集数据) |
| 质量损失 10-15%、五维均分 3.7 vs 3.9 | 回答命中率 / 拒答准确率(无落地数据) |
| L1 963 字符替代 PRD 4558 字符 | AI 预审采纳率(忽略无痕删、分母不可复原) |
| L3 零模型成本,4 需求沉淀 8 条偏好 | 「598 用例」→ 实测全量约 3344 |
| LightRAG bm25 43 个单测 + 全量回归 | 三个未实测指标(精确词召回/平坦扩容/P95 延迟) |
| 5 种工具(主链路挂 4 种) | 「逐级压缩先历史后文档」/「定时批量抽取」/「prompt 版本化治理」 |

### 4.6 一句话总结(审查一最后那句,面试官最想听)

> **「我遇到了 XXX 问题,所以这样设计,权衡是 XXX」**——上面每一条都按这个格式准备。被问到任何一条,先说痛点、再说设计、最后讲「为什么不那么做」。

---

*使用提示:面试前一晚把 4.1 ~ 4.4 四张防守卡过一遍,重点背 4.3(并发取舍)和 4.1(Agent 运行时)——这两块是「诚实讲得好 = 加分,吹过头 = 当场穿帮」的高危区。*