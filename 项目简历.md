# 个人简历 · 转正答辩

> **目标职级**:校招 / 初级 → 中级工程师(Java 后端 + AI Agent 应用开发)
> **项目角色**:全栈贯通,Java 后端为主的端到端核心开发

---

## 一、项目一句话定位

**把「一个需求从创意到上线」原本散落在文档/IM/表格里的人肉协作,收拢成一条「结构化需求流水线」——每个环节有明确状态、有权限、防并发冲突,关键节点内嵌 AI Agent 代替人工起草/精修/评审,配分层记忆让 AI 越用越懂用户。**

技术栈:Java 17 · Spring Boot · Spring AI(ReAct Agent)· MySQL + Redis(JIM)双存储 · React 18 / TypeScript / Vite / Electron

---

## 二、核心贡献(每条 = 真实痛点 + 方案 + 为什么这么做 + 结果)

### 1. 需求流程失控:前端门禁可被绕过、状态边界散乱 → 服务端状态机收口

**问题**:idea→demo→prd→内审→评审→trd→trd评审→任务→交付十多个环节,早期推进全凭前端 `if/else` 按钮门禁,绕过 UI 直接调接口就穿透,谁推进了什么、能不能回退都没有权威约束。

**方案**:后端 `WorkflowTransitionService` 统一收口——
- 单一入口 `transition(action)` 分派 confirm/skip/pass/return,每个动作只改当前阶段语义字段;
- `guardTransition()` 四层服务端鉴权(超管白名单 / OWNER / 编辑者成员表 / 评审名单),前端门禁只做体验;
- `returnForModify()` 评审打回重改,清投票意见但**保留 TRD/任务等后续产物**;
- 三类异常语义分离:不支持动作 400000 / 业务前置不满足 500001 / 越权 403000(带 owner 引导申请权限)。

**结果**:流转变成可审计、可回退、可扩展(加一个流转 = 加一个动作分支)的受控流程,前后端按统一 stageStatus 四值对齐。

---

### 2. 多人/多线并发改同一条需求互相覆盖 → 乐观锁 + 冲突显式化

**问题**:一条需求被多个协作者同时编辑,且 AI 多线工具会并发回写同一需求,无保护则后写者静默覆盖先写内容。

**方案**:
- **版本乐观锁(CAS)**:文档保存走 `UPDATE...WHERE id=? AND version=?`,命中 0 行抛冲突;**AI 工具回写与人工编辑走同一条 CAS 链路、递增同一 version**,AI 回写不覆盖人工编辑;
- **冲突按来源分级解决**:409 后先 diff 正文相对服务端基线是否被改——状态类冲突(投票/流转 bump 的 version)静默重试不打扰;正文类冲突走 **PRD 章节级三方合并**(基线/我/服务端按章节 id 对齐):改不同章节自动合并双方修改、零打扰静默提交,同章冲突才弹对比框逐章裁决,杜绝「后写者静默覆盖先写者」;
- **写路径分级取舍**:流程流转是幂等意图,有意走 last-write-wins + 业务前置校验兜底——这是明示 trade-off,不是疏漏;
- **原子编号**:`req_sequence` + `INSERT IGNORE` 初始化 + `LAST_INSERT_ID(current_value+1)` 行级原子自增,并发创建不重号。

**结果**:文档写入从「悄悄丢数据」变「显式冲突 + 可恢复 + 可合并」;CAS 与 last-write-wins 按场景分级,协作与高并发回写都安全——多数正文冲突(不同章节)自动合并无感通过,真冲突由用户逐章裁决,双方编辑都不丢。

---

### 3. 主库故障/新表迁移拖垮读链路 → 双存储降级 + 读口隔离

**问题**:读接口一旦主库异常整页白屏;评论/投票拆独立表后,新旧并存期间新表 bug 会拖垮原有读口。

**方案**:
- 读写主走 MySQL,所有入口 try-catch 后自动 fallback 到 Redis(JIM)缓存,主库抖动页面可读、核心数据不丢;
- `overlayComments()` 读出后覆盖旧字段,前端结构零改动;新表查询失败保留原快照字段,绝不因新表破坏读口。

**结果**:主库故障、新表异常都不再打断协作链路,拿到「关键数据不丢 + 读链不破」的容错能力。

---

### 4. AI「说得漂亮但不可控」→ Agent 编排运行时产品化

**问题**:① 模型只能回答不能真的改文档;② 长文档撑爆 token 且费钱;③ 用户「写 PRD / 改这段 / 随便聊」走同一条路分不清。

**问题**:① 模型只能回答不能真的改文档;② 长文档撑爆 token 且费钱;③ 用户「写 PRD / 改这段 / 随便聊」走同一条路分不清;④ 把控制流整体交给模型会不可控——一次判错就是一次真实写库。

**方案**:采用「**确定性工作流编排 + 受限 ReAct 执行**」双层架构,而非一个大 Agent 自主跑全流程——

**① 编排层(确定性 Java 代码,模型不参与控制流)**:`IdeaAgentService.handle()` 显式编排意图→知识检索→按意图分流(intent 分类 + 置信度阈值 + 三层兜底 + 30s 硬超时,判不准宁可当聊天),每节点开始/结束/耗时经 `agentProgressEmitter` 下发前端进度条;分支间自动兜底(doc-edit 无产出自动转对话)——**最坏情况有界,模型判错最多走错分支,不会失控跑飞**。

**② 执行层(只在需要模型自主判断的两个分支开工具循环)**:
- **改文档分支 → ReAct 循环**:ReactAgent(StateGraph 条件边)实现「推理→调工具→观察→再推理」;`DocEditTools` 把编辑动作建模成 5 种结构化工具(replace/append/patch_lines/rewrite/reply_only),主链路挂 4 种;**模型只声明工具意图,真实执行由业务层解析 diff + 人工确认后落库**,杜绝模型自由文本直接改数据——编辑落库必须过人这关,循环深度天然有界;
- **对话分支**:ReAct + 知识库工具,模型自主判断要不要查;
- **文档生成**不走 ReAct:纯文本生成无工具需求,直接网关流式,省 token 和首字延迟(并绕开框架流式 bug)。

**③ 横切可靠性**:**上下文预算**两段式截断(超预算中段省略→尾部硬截断)→400 响应式激进压缩兜底(60% 水位);**RAG** 需求关联文档向量化(chunk 1000/overlap 100),针对不支持 filter 下推改纯向量+应用层过滤+防霸榜;**可观测**记录每请求 requestId/模型/prompt 版本/耗时/TTFB。

**结果**:把「用户想干什么」交给确定性代码、「怎么改」交给受限 ReAct、「控制流不放给模型」——AI 从「会聊天的外挂」变成「长在流程里、能真正改文档、可控可查可回退」的生产级 Agent。

---

### 5. AI 每次从零开始 → 分层记忆系统(L0-L3)

**问题**:TRD 生成把 PRD 整段注入当背景,单次 user token 达 **14211**;跨需求稳定偏好零记忆,每个需求都要重复交代。

**方案**:L0 原始证据 → L1 需求级原子记忆 → L3 跨需求偏好三层设计,L1 按 requirementId 隔离抽取「决策/约束/事实/待确认」原子,L3 词表+频次阈值零模型成本提炼,注入 L1 最顶 + 双层 fail-open。

**结果与评估(本项目的量化证据)**:同需求 + 强制新会话 + 修复 PRD 隐藏泄漏路径做对照实验(L1 963 字符替代 PRD 4558 字符),**token 14211→8246 省 42%**,质量损失 10-15%(集中在数值精度)、核心架构 100% 一致;实测「L1 很全时 L3 边际增益为 0」,据此界定 L3 价值场景为新需求初期——两条记忆信息正交、不无脑叠加。

---

### 6. AI 工程细节:意图路由 / 上下文预算 / 结构化输出的具体取舍

这三条是第 4 点「Agent 运行时」里的关键实现细节,单独讲清「问题 → 怎么改」:

- **意图路由 `IdeaIntentRouter`**:「就按你说的改」这类指代意图模型容易判错。方案 = LLM 分类 + 置信度阈值(低于阈值兜底 chat)+ 解析失败兜底 + 30s 硬超时,三层兜底保证判不准时宁可当聊天、不误触整篇重写。
- **上下文预算 `ContextTokenGuard`**:PRD/TRD 几千字会撑爆 token。方案 = 两段式截断(超预算先对最长 user 消息零成本中段省略,仍超限尾部硬截断循环压到达标)+ 400 响应式激进压缩兜底(60% 水位),逐段降级而非一刀切。
- **结构化输出 `chatForObject`**:模型自由文本会污染下游数据结构。方案 = 强类型反序列化 + 容错解析(JSON 截断抢救、裸引号修复),格式约束写进 prompt,解析失败捕获后回落默认分支,失败不中断主链路。

---

## 三、其他工程贡献

- **多模块分层**:main→web→app→client 单向依赖,Controller 薄壳、业务在 app、契约在 client;
- **统一响应/异常**:`Result<T>` + `GlobalExceptionHandler`,全链路 traceId;
- **前端**:React + Vite 需求详情、状态机驱动渲染、投票轮询、SSO 登录。

---

## 四、落地成果

- 需求全生命周期线上化:Idea/PRD/TRD 自动生成 + 人工精修,评审投票、评论、AI 参数记录全平台闭环;
- 分层记忆上线:L1 替代 PRD 注入后单次生成 **token 节省 42%**,L3 从 4 个历史需求零成本沉淀 8 条偏好;
- 生产/预发多环境(K8s)部署,环境按域名区分需求编号前缀。

> 说明:内部新平台暂无对外量化数据(GMV/人日),价值以功能完整、工程规范、稳定容错为准。

---

## 五、总结

从 0 到 1 参与「AI 协作 · 需求全链路平台」:把需求流转做成受控**状态机**,把并发容错做成可靠**乐观锁 + 双存储降级**,把 AI 做成能真正改文档的 **Agent 编排运行时**,再用**分层记忆**把上下文成本降 42% 并积累跨需求偏好。核心是——**不是「做个 AI 工具」,而是把研发流程固化成系统、再把 AI 长进系统的固定环节里**。

---

*答辩前把《面试详述稿.md》过一遍:深挖分层记忆 + Agent 运行时两条亮点,备好并发取舍、Agent 运行时等防守卡话术。*