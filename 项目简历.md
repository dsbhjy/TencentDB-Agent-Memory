# 个人简历 · 转正答辩

> **目标职级**：校招 / 初级 → 中级工程师（Java 后端 + AI Agent 应用开发）
> **项目角色**：全栈贯通，Java 后端为主的端到端核心开发

---

## 一、项目一句话定位

**把「一个需求从创意到上线」原本散落在文档、IM、表格里的人肉协作，收拢成一台「结构化需求流水线」——每个环节有明确状态、有权限、防并发冲突，并在关键节点内嵌 AI Agent 代替人工起草/精修/评审，配合分层记忆让 AI 越用越懂用户。**

技术栈：Java 17 · Spring Boot · Spring AI（ReAct Agent）· MySQL + Redis 双存储 · React 18 / TypeScript / Vite / Electron

---

## 二、核心贡献（每一条 = 一个真实业务问题 + 技术方案 + 结果）

### 1. 需求流程失控：前端门禁可被绕过、状态边界散乱 → 用「服务端状态机」收口

**问题**：需求要经历 idea → demo → prd → 内审 → 评审 → trd → trd评审 → 任务 → 交付 十多个环节。早期推进全靠前端 `if/else` 判断哪个按钮能点。但前端门禁**绕过 UI 直接调接口就能穿透**，谁在哪个环节推进了什么、能不能回退，完全没有权威约束，出了错只能靠人工查。

**方案**：把全链路建模成显式状态机，由后端 `WorkflowTransitionService` 统一收口：

- **单一入口 + 动作分派**：所有流转走一个 `transition(action)`，按动作分发到 `confirm / skip / pass / return` 系列方法，每个动作只改当前阶段的语义字段。
- **服务端鉴权**：`guardTransition()` 在后端校验「owner / 超管 / 编辑者 / 命中内审评审名单」才能推进——前端判断只做体验，真正的门禁在后端，直接调接口也绕不过去。
- **显式回退**：`returnForModify()` 支持「已通过评审的需求打回重改」，清空投票意见但**保留后续产物（TRD/任务）**，状态置回待评审。
- **非法流转友好报错**：区分「链路已完成重复提交」与「当前节点不对」，避免误导性报错。

**结果**：需求流转边界变成**可审计、可回退、可扩展**(加一个流转=加一个动作分支)的受控流程；前后端按统一 `stageStatus` 对齐，任何一次推进/回退都有服务端权威记录。

---

### 2. 多人/多线并发改同一条需求，互相覆盖 → 用「乐观锁 + 冲突显式化」解决

**问题**：一条需求会被**多个协作者同时编辑**，且 **AI 多线工具会并发回写同一需求**。如果没有保护，后写的人会**静默覆盖**先写的人/AI 刚写好的内容——这是协作平台最隐蔽也最致命的 bug。

**方案**：

- **版本乐观锁**：每条需求带 `version` 字段，`updateWithOptimisticLock()` 在事务里比对「客户端带过来的版本」与「库里当前版本」，不一致抛 `RequirementVersionConflictException`；前端收到冲突后**以后端最新为准重新加载**，而不是盲目覆盖。不靠加锁排队，天然适合「高频读、低冲突」的协作场景。
- **原子编号**：需求编号用 `req_sequence` 表 + `LAST_INSERT_ID(current_value+1)` 这一 **行级原子自增**，并发创建也不会出现重号。

**结果**：并发写入从「悄悄丢数据」变成「显式冲突 + 可恢复」，多人协作与 AI 并发回写都能安全进行。

---

### 3. 主库故障/新表迁移会把整条读链路打挂 → 用「双存储降级 + 读口隔离」兜底

**问题**：需求的读接口（列表/详情/创建/更新）一旦主库异常，用户就**整个页面白屏**；而且项目后期把「评论/投票」从 payload JSON 拆到独立表，**迁移期间新旧表并存**，新表稍有 bug 就可能拖垮原有读口。

**方案**：

- **双存储自动降级**：读/写主走 MySQL，所有入口 `try-catch` 后自动 fallback 到 **Redis（缓存）**，保证主库抖动时核心数据不丢、页面可读。
- **读口隔离 + 可降级**：新表数据通过 `overlayComments()` **在读出后覆盖**旧字段，前端结构零改动；整体包 try-catch，新表查询失败时**保留原快照字段**，绝不因新表问题破坏读口。

**结果**：主库故障、新表异常都不再能打断用户的协作链路，系统拿到「关键数据不丢 + 读链不破」的容错能力。

---

### 4. AI「说得漂亮但不可控」：能力不可插拔、易撑爆上下文、分不清用户意图 → 用「Agent 编排运行时」产品化

**问题**：直接把 LLM 接到对话里，会出现三个坑——① 模型只能「回答」不能「真的去改文档」，说了一堆正确的建议但落不了地；② 长文档（PRD/TRD 动辄几千字）会把 token 上下文撑爆、还费钱；③ 用户说「帮我写个 PRD」和「这段帮我改一下」和「随便聊聊」走的是同一条路，AI 分不清该生成、该编辑、还是该聊天。

**方案**：基于 Spring AI 封装 **Agent 运行总线**，把「生成式模型」升级成「会调用工具的 Agent」：

- **ReAct 工具调用闭环**：模型在「推断 → 调用工具改文档 → 看到结果 → 再推断」循环里真正落地。`DocEditTools` 把编辑动作建模成 5 种结构化工具——`replace_section`（整节替换）/ `append_section`（追加）/ `patch_lines`（定点改行）/ `rewrite_document`（整篇重写）/ `reply_only`（只聊不改），模型每次 tool call 都被解析成明确落库的编辑操作，而不是一段靠不住的自由文本。
- **上下文预算 `ContextTokenGuard`**：Prompt 超限时渐进压缩，把长文档调用控制在 token 预算内——这是 LLM 生产落地最容易翻车、也最没人提的一刀。
- **意图路由**：进入 generate/chat 前先由 `IdeaIntentRouter` 判断用户是「要生成文档」「要改文档」还是「纯聊天」，三种走向不同链路，避免「随手聊一句」误触发整篇重写。
- **RAG 检索**：文档生成时按需求背景向量检索历史相关文档（`SpringAiRagFacade`），并**针对 Vearch 向量库不支持 filter 下推的坑**，改成「纯向量检索 + 应用层兜底过滤」，让 AI 吃的是内部知识底座而非胡编。
- **可观测**：每次调用记录 requestId / 模型 / 提示词版本 / 耗时，出问题时能定位「为什么这次 AI 产出不对」。

**结果**：AI 从「会聊天的外挂」变成「长在流程里、能真正改文档、可控可查可回退」的**生产级 Agent 能力**。

---

### 5. AI 每次生成都从零开始：上下文贵、偏好记不住 → 用「分层记忆系统（L0-L3）」让 Agent 越用越懂用户

**问题**：AI 生成 TRD 时靠整段注入历史 PRD 当背景，单次 user token 高达 14211；且 AI 对用户跨需求的稳定偏好（技术选型、文档风格）零记忆，每个新需求都要人工重复交代。

**方案**：设计三层记忆——**L0 原始证据 → L1 需求级原子记忆（按 requirementId 隔离）→ L3 跨需求用户偏好**：

- **L1 替代 PRD 注入**：把千字级 PRD 压缩成结构化原子记忆，生成时按 requirementId 精准取用。
- **L3 零模型成本提炼**：关键词/归一化匹配 + 跨需求聚合 + 频次阈值（≥2 需求 = candidate，≥3 需求 = auto_confirmed），不调模型，4 个历史需求沉淀 8 条偏好（4 confirmed + 4 candidate）；注入生成链路带指纹日志、fail-open。
- **严格对照实验验证**：同需求 + 强制新会话（排除会话记忆污染）+ 修复 context JSON 隐藏泄漏 PRD 的路径，盲评 + 关键词覆盖 + 污染监测三重判据。

**结果**：**L1 结构化记忆（963 字符）替代 PRD 注入段（4558 字符），token 节省 42%**，产物体积几乎相同，核心架构 100% 一致、质量损失仅 10-15%（集中在数值精度）；并实测「L1 很全时 L3 注入边际增益为 0」，据此确认 L1（本需求事实）与 L3（跨需求模式）信息正交、维持叠加注入，把 L3 价值场景定位在 L1 薄/空的新需求初期。

---

## 三、其他工程贡献

- **多模块分层**：main → web → app → client 单向依赖，Controller 薄壳、业务在 app、契约在 client。
- **统一响应/异常**：`Result<T>` 统一包装 + `GlobalExceptionHandler`，链路带上 traceId 便于排障。
- **前端**：React + Vite 需求详情、状态机驱动渲染、投票轮询、SSO 登录链路。

---

## 四、落地成果

- 需求全生命周期线上化：Idea / PRD / TRD 自动生成 + 人工精修，入库即结构化；评审投票、评论、AI 参数记录全平台闭环。
- 分层记忆系统上线：L1 替代 PRD 注入后单次生成 **token 节省 42%**；L3 从 4 个历史需求零成本沉淀 8 条用户偏好并注入生成链路。
- 生产/预发多环境（K8s 部署平台）部署，环境按域名区分需求编号前缀（如 `REQ-P-2026-000001`）。

> 说明：内部新平台暂无对外量化数据（GMV/节省人日），价值以**功能完整、工程规范、稳定容错**为准。

---

## 五、总结

从 0 到 1 参与了「AI 协作 · 需求全链路平台」：既把需求流转做成受控的**状态机**、把并发与容错做成可靠的**乐观锁 + 双存储降级**，又把 AI 做成能真正改文档、可控的 **Agent 编排运行时**，再配上**分层记忆系统**让 AI 的上下文成本降 42%、并开始积累跨需求用户偏好。核心是——**不是「做个 AI 工具」，而是把研发流程固化成系统、再把 AI 长进系统的固定环节里**。

---

*答辩前把「项目解释.md」过一遍，确保每个技术点都能用大白话 + 一个具体例子讲清「问题是什么、我为什么这么做、不这么做会怎样」。*