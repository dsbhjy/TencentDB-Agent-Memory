# 分层记忆设计模板

- 日期：{{日期}}
- 项目：{{项目名}}
- 范围：{{本阶段覆盖的层和场景}}
- 状态：草案 / 实现中 / 已验证
- 关联：[分层记忆设计方法论.md](./分层记忆设计方法论.md)

> 本模板从方法论提炼，拷贝后按项目实际情况填充。带 `{{}}` 的是占位，`【】`是填写指引。

---

## 1. 背景与目标

### 1.1 一句话

> {{用一句话说清本层记忆是什么、给谁消费}}

### 1.2 为什么不能复用现有表

【列出项目里已有的、看似能当记忆用的表/存储，逐一说明为什么不能直接用。这一步防止重复造轮子，也防止把业务表当记忆表踩坑。】

| 现有存储 | 职责 | 为什么不能当记忆层 |
| --- | --- | --- |
| {{表名}} | {{职责}} | {{不能用的理由：会被删 / 未清洗 / 带窗口 / 是模型上下文}} |

### 1.3 分层目标边界

【按方法论的优先级铁律，先需求内同步，后跨需求偏好。】

- 第一优先级：同一 {{requirement_id}} 下，{{模块A}} / {{模块B}} / ... 之间共享背景、决策、约束；
- 第二优先级：从多个需求中提炼稳定的用户偏好；
- 不把单个需求事实直接升格为用户偏好。

### 1.4 架构约束对齐

【列出项目架构约束（如统一底座、不引入新 SDK），说明本设计如何符合。】

---

## 2. 范围与非目标

### 2.1 本阶段范围

1. {{捕获链路}}；
2. {{干净文本规则}}；
3. {{幂等写入}}；
4. {{落库方式}}；
5. {{下游触发预留}}；
6. {{开关与灰度}}。

### 2.2 非目标（明确不做）

- ❌ {{下一层才做的事}}；
- ❌ {{向量化 / 图谱 / 编辑面板}}；
- ❌ {{非主链路捕获}}；

---

## 3. L0 原始证据层

### 3.1 捕获点

【选主链路里"用户输入与最终输出同帧可得"的位置，通常是生成完成事件处。】

- 捕获点：{{Controller / Service / 事件}}
- 时序：
```text
生成完成
  → 发送完成事件
  → 异步捕获（专用线程池）
  → sanitize → 幂等写入
  → complete
```

### 3.2 捕获字段

| 字段 | 来源 | 说明 |
| --- | --- | --- |
| user_text | request.message | sanitize 后的真实用户输入 |
| assistant_text | 优先取完整产物，无产物兜底 message | 助手最终产出 |
| {{归属维度}} | request/response | agent_key / requirement_id / stage / session_id |
| env | 当前 profile | 环境隔离 |

### 3.3 干净文本规则（防污染清单）

绝不写入 user_text / assistant_text 的内容：
1. system prompt 与业务系统提示词；
2. 已注入的记忆（防反馈循环核心）；
3. RAG / 知识库检索原文；
4. harness 包装噪声（system-reminder 等）；
5. 前端页面 JSON 大上下文；
6. 敏感信息（凭证、Cookie、密钥）。

### 3.4 幂等与生命周期

- 幂等键：`turn_id = env + user + session + run`
- 辅助指纹：`SHA-256(text)`
- 生命周期：业务会话 reset 不删 L0

---

## 4. L1 原子记忆层

### 4.1 记忆类型

【按方法论六类，可裁剪。】

| 类型 | 含义 | 本项目示例 |
| --- | --- | --- |
| fact | 客观事实 | {{}} |
| decision | 已定决策 | {{}} |
| constraint | 约束/口径 | {{}} |
| risk | 风险 | {{}} |
| open_question | 待确认项 | {{}} |
| task_hint | 任务提示 | {{}} |

### 4.2 抽取流程

```text
L0 capture 完成 → notify_status=pending
  → L1 consumer 扫描 pending
  → LLM 结构化抽取 atoms
  → 应用层机械防护（白名单/必填/大段原文/可追溯/作用域）
  → upsert L1
  → 回写 L0 notify_status = done/skipped/failed
```

### 4.3 去重设计

- dedup_key = `hash(env + requirement + memory_type + normalized(subject))`
- **不纳入 predicate**（不稳定谓词会逃逸去重）
- normalized = trim + 折叠空白 + 小写

### 4.4 注入设计

【关键决策：L1 注入的目标场景是什么？补充原文 / 替代原文 / 跨模块复用。参考方法论 §3.5。】

- 注入位置：{{基础上下文之后、前序原文之前/之后}}
- 注入格式：紧凑 markdown，按类型分组
- 故障隔离：fail-open（仓库不可用/异常 → 返回空，不阻断主流程）
- 指纹日志：记录 是否注入 / 记忆长度 / 是否含独有标记

---

## 5. 数据模型

### 5.1 L0 表

```sql
CREATE TABLE {{l0_table}} (
  id BIGINT AUTO_INCREMENT PRIMARY KEY,
  env VARCHAR(16) NOT NULL,
  user_erp VARCHAR(64) NOT NULL,
  requirement_id VARCHAR(64) NOT NULL,
  -- {{归属维度字段}}
  session_id BIGINT,
  run_id VARCHAR(64),
  turn_id VARCHAR(128) NOT NULL,
  user_text TEXT,
  assistant_text TEXT,
  status VARCHAR(32),
  notify_status VARCHAR(32) DEFAULT 'pending',
  message_fingerprint VARCHAR(128),
  created_at DATETIME(3) DEFAULT CURRENT_TIMESTAMP(3),
  UNIQUE KEY uniq_turn (turn_id),
  KEY idx_requirement (env, requirement_id),
  KEY idx_notify (notify_status)
);
```

### 5.2 L1 表

```sql
CREATE TABLE {{l1_table}} (
  id BIGINT AUTO_INCREMENT PRIMARY KEY,
  env VARCHAR(16) NOT NULL DEFAULT 'dev',
  user_erp VARCHAR(64) NOT NULL,
  requirement_id VARCHAR(64) NOT NULL,
  -- {{归属维度}}
  source_l0_id BIGINT NOT NULL,        -- 可追溯硬约束
  source_turn_id VARCHAR(128) NOT NULL,
  memory_type VARCHAR(32) NOT NULL,     -- 白名单
  subject VARCHAR(256) NOT NULL,
  predicate VARCHAR(128),
  object_value TEXT,
  content TEXT NOT NULL,
  module VARCHAR(64),
  scope VARCHAR(32) NOT NULL DEFAULT 'requirement',  -- 作用域硬约束
  confidence DECIMAL(4,3) DEFAULT 0.800,
  status VARCHAR(32) DEFAULT 'active',
  dedup_key VARCHAR(128) NOT NULL,
  created_at DATETIME(3) DEFAULT CURRENT_TIMESTAMP(3),
  updated_at DATETIME(3) DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
  UNIQUE KEY uniq_dedup (dedup_key),
  KEY idx_req_type (env, requirement_id, memory_type),
  KEY idx_source_l0 (source_l0_id)
);
```

---

## 6. 验证计划

### 6.1 机制验证（注入确实发生）

- 指纹日志法：记录注入状态 + 独有标记，排除原文干扰。

### 6.2 质量验证（A/B/C 对照）

| 组 | 记忆 | 前序原文 | 目的 |
| --- | --- | --- | --- |
| A | 关 | 开 | 基线 |
| B | 开 | 开 | 边际贡献 |
| C | 开 | 关 | 独立支撑能力 |

### 6.3 验收标准

1. {{可量化的验收项，如：一篇文档可抽取出 N-M 条 atom}}；
2. 每条 atom 可追溯到 source_l0_id；
3. 重复消费同一 L0 不产生重复 atom；
4. 注入机制用指纹日志证实；
5. 对照实验结论如实记录（有正贡献 / 无正贡献 / 重新定位）。

---

## 7. 已知局限与改进方向

【MVP 跑通后如实记录问题，按优先级排列。】

### 7.1 {{问题名}}（{{优先级}}）
- 现象：{{}}
- 根因：{{}}
- 已做 / 待做：{{}}

---

## 8. 检查清单（设计完成前过一遍）

**职责边界**
- [ ] 每层职责单一，不重叠
- [ ] 与现有表区分清楚，不重复造轮子
- [ ] 作用域显式区分（requirement vs user）

**L0**
- [ ] 存干净产物，不存原始请求
- [ ] 防反馈循环（已注入记忆不回写）
- [ ] 幂等键覆盖重放/重试/断连
- [ ] 旁路捕获，不反压主流程
- [ ] 生命周期独立于业务会话

**L1**
- [ ] 每条 atom 可追溯到 L0（硬校验）
- [ ] memory_type 白名单
- [ ] 大段原文防护
- [ ] 去重键不含不稳定字段（predicate）
- [ ] 注入 fail-open
- [ ] 注入目标场景想清楚（补充/替代/跨模块）

**验证**
- [ ] 不用混淆判据（产物承接前序 ≠ 记忆生效）
- [ ] 指纹日志确认开关状态
- [ ] A/B/C 对照变量干净
- [ ] 结论如实记录，无正贡献不强行宣称成功

**工程**
- [ ] 数据源隔离 + 降级
- [ ] 配置开关分离
- [ ] 异步有界线程池
- [ ] env 隔离，不硬编码
