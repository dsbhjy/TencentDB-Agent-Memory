# 项目解释.md —— 给初级 Java / AI 实习生的技术讲解

> 本文用「大白话 + 生活类比 + 最小代码示例」讲清简历里四个核心难点。
> 目标:你读完能照着讲给别人听,面试官提问时你心里有底。
> 四个重点:
> - **① 需求工作流状态机**(流程怎么建模、别人怎么绕不过去)
> - **② AI Agent 编排与工具调用**(让大模型"真正动手改文档")
> - **③ 乐观锁与并发容错**(多人 + AI 同时改同一份数据不乱)
> - **④ 分层记忆系统 L0-L3**(让 AI 上下文便宜 42%、还记住你的偏好)
>
> 每个难点都按「**为什么有问题 → 怎么解决 → 不解决会怎样**」讲。

---

## 〇、先看懂整个系统在做什么

想象你是公司里负责「把一个创意变成上线需求」的流程负责人。以前:

- 有人用 Word 写 Idea → 发群里 → 有人评审 → 有人写 PRD → 再评审 → 写 TRD → 排期 → 开发 → 交付。
- 这中间哪个文档是最新版、谁看完还没回复、AI 能不能帮我起草?全靠人肉维护。

这个项目就是把流程**搬上系统 + 让 AI 帮忙**:

> 系统里一条需求会经历 10 个阶段,像流水线上的工位一步步走:
> **Idea(创意)→ Demo(演示)→ PRD → PRD 内审 → PRD 评审 → TRD → TRD 内审 → TRD 评审 → 任务(Tasks)→ 交付(Delivery)**

每个阶段有执行人、完成/进行中/阻塞等状态。每次有人点"确认",系统就自动推进/记录;AI 在每个环节帮你起草文档、做分析。

---

## 一、需求工作流状态机(难点 ①)

### 1. 为什么需要状态机(真实痛点)

一条需求在生命周期里状态会依次变化。如果**把这些判断都放在前端** `if/else` 里,会出两个致命问题:

```java
// 散落的前端判断(❌ 问题所在)
if (stage == "prd" && 当前用户是 owner) { 显示"通过"按钮 }
```

1. **前端门禁可被绕过**:按钮点不了 ≠ 不能操作。有人用浏览器控制台、或直接调后端接口,就能**跳过前端判断**推进/回退需求——前端只管「显示什么按钮」,它管不住「谁真的能操作」。
2. **状态边界散乱**:每个环节「能不能进下一步」的规则散落各处,加一个阶段要改一堆地方,出错只能人工查。

**不解决会怎样**:需求流转变成谁都能改、改了什么没权威记录,流程形同虚设。

### 2. 怎么解决:后端状态机收口

**状态机思路**:把「当前在哪一步 + 下一步合法动作」用统一数据结构和一张「动作表」表达;改变 = 走一个明确动作,而不是散落的 if。

- 阶段先用有序列表定义(相当于地图上的站点):

  ```java
  private static final List<String> FLOW_STAGES = List.of(
      "idea", "demo", "prd", "internalReview", "review",
      "trd", "trdInternalReview", "trdReview", "tasks", "delivery");
  ```

- 后端只提供**一个入口** `transition()`,接收「动作名」,内部用 switch 找到对应处理函数:

  ```java
  Map<String, Object> patch = switch (action) {
      case "CONFIRM_IDEA"     -> confirmIdea(requirement);
      case "PASS_REVIEW"      -> passReview(requirement, operator);
      case "RETURN_TO_PRD"    -> returnForModify(requirement, "prd");
      ...
  };
  ```

  每个动作只负责一件事:把该阶段字段改成「已通过 / 已跳过 / 待重新评审」。

### 3. 两个关键设计(面试爱考)

- **后端守卫身份 `guardTransition`**:前端按钮可被绕过,所以后端在每次流转前做**身份校验**——只有 owner / 超管 / 编辑者 / 命中评审名单的人才能推进。

  > 类比:进 VIP 房间,门口保安(后端)再查一次身份证,不能只看前台(前端)放行。

- **回退能力 `returnForModify`**:需求已通过评审,但后来发现要改。状态打回对应阶段,**清空投票/意见,但保留 TRD、任务等后续产物**,改完由 owner 重新发起评审。

> 价值:你面试可答——状态机让**流转边界清晰、可扩展**(新增阶段/动作只加分支),而且**服务端是唯一权威**,直接调接口也绕不过去。

---

## 二、AI Agent 编排与工具调用(难点 ②)

### 0. 先立住主叙事:双层架构(这是整个 Agent 部分的骨架)

很多 Agent 项目是「一个大 Agent 自主管全流程」——把控制流也交给模型。这个项目反着来:**控制流是确定性 Java 代码,模型自主性只放在两个被收口的循环里**。

```
用户消息
  │
  ▼
┌─ 编排层(确定性工作流,Java 代码)──────────────────┐
│  intent 节点(LLM 只做分类,决策仍由代码执行)        │
│  knowledge 节点(确定性检索,代码决定要不要发)       │
│      │                                            │
│      ├─ 生成文档 → 直接网关流式(无工具循环)          │
│      ├─ 修改文档 → ReAct 循环(5 种编辑工具)         │ ← 模型自主性只住这里
│      └─ 对话     → ReAct 循环(知识库工具)           │ ← 和这里
│                     ↓ 无产出 → 兜底转对话           │
└───────────────────────────────────────────────────┘
```

**为什么这样分**:「用户想干什么」(意图/分支/降级)交给模型自由发挥,判一次错就是一次真实写库;确定性编排让最坏情况有界。而「改哪一章、要不要查知识库」这类单轮内判断,模型比规则准——所以 ReAct 循环只在**编辑文档、知识检索**两个分支开;文档生成不需要工具,开循环纯属浪费 token 和首字延迟。

### 1. 大模型的"先天缺陷"(真实痛点)

LLM 只能"理解文字、生成文字",它**不能自己查库、改文档、调接口**——就像一个只会写纸上操作说明、没法动身的军师。

这带来三个真实的坑:

1. **说了做不到**:AI 能洋洋洒洒告诉你「PRD 第二段应该改成……」,但它改不了系统里的文档,得人工复制粘贴。
2. **长文档会撑爆上下文**:LLM 一次能吞的 token 有硬上限,PRD/TRD 动辄几千字,超了就报错或丢信息,还费钱。
3. **分不清用户意图**:用户说「帮我写个 PRD」「这段改一下」「随便聊聊」,如果走同一条路,可能「随口聊一句」就误触发整篇重写。

**不解决会怎样**:AI 永远只是「贴在对话框上的聊天机器人」,提的效很有限。

### 2. 执行层:把「军师」配「手脚」,但手脚听人的(受限 ReAct 循环)

要让 AI 真正干活,需要给军师配工具。ReAct 循环本身不难:

```
AI 思考(该改哪段)→ 决定调用工具(改文档)→ 收到结果 → 再思考 → 直到完成
```

**这个项目的关键不在「有循环」,在「循环被收口」**:

- **把「改文档」拆成 5 种结构化工具 `DocEditTools`**:`replace_section` 整节替换、`append_section` 追加章节、`patch_lines` 定点改某几行、`rewrite_document` 整篇重写、`reply_only` 只聊不改(局部编辑链路挂载 4 种,整篇重写因流式时长未启用)。
- **工具函数本身是占位(noop)**——模型声明「我要调 replace_section」之后,真正执行的是业务层:解析成明确编辑操作 → 生成 diff → **人工确认后才落库**。模型从头到尾**没有直接改数据的能力**,幻觉风险兜在「人」这一层。
- 因为工具结果是业务层回的固定确认,模型通常一轮就结束——循环深度天然有界,不是刻意设限,是收口带来的自然结果。

### 3. 编排层:意图路由 + 节点化 + 分支兜底

- **意图路由**(解决坑 3):`IdeaIntentRouter` 用 LLM 分类判断用户是「要生成文档」「要改文档」还是「纯聊天」,走三条不同链路,避免「随口聊一句」误触发整篇重写。注意:**模型在这里只出分类结果,走哪条分支、怎么走,是 Java 代码决定的**——分类错只影响单轮路由,不会让流程失控。
- **三层兜底**:置信度低于阈值→当聊天;解析失败→当聊天;30s 硬超时+禁重试防挂死——判不准时宁可当聊天,不误触重写。
- **节点化进度**:每个节点(intent/检索/生成/修改/回复)的开始/结束/耗时经 emitter 下发前端,用户看到「正在理解意图→正在生成文档→已生成」的进度条——Agent 过程可观测,不是黑盒转圈。
- **分支间兜底**:编辑分支没产出写入?编排层自动落到对话分支回复。每一层都有下一步,用户永远不会拿到空响应。

### 4. 横切两层的可靠性(解决坑 2 + 通用兜底)

- **上下文预算 `ContextTokenGuard`**:对超长 prompt 做**两段式截断**——先对最长 user 消息零成本中段省略,仍超限尾部硬截断循环压到达标,再加 400 响应式激进压缩兜底;LLM 摘要层预留未启用(避免递归调网关)。
- **RAG 检索**:`SpringAiRagFacade` 把需求关联文档按标题分块向量化,检索相关片段进上下文,答案可溯源。(附加:Vearch 向量库不支持 filter 下推,改走「纯向量检索 + 应用层 requirementId 过滤」。)
- **结构化输出**:强制 AI 返回能被 Java 直接反序列化的 JSON(`chatForObject` + `AiStructuredOutputConverter`),解析失败捕获后回落默认分支,不中断主链路。

> 面试对答:
> - Q:为什么不让一个大 Agent 自主管全流程? A:控制流交给模型,判一次错就是一次真实写库;确定性编排把最坏情况限定在单轮路由错误,且每个分支都有兜底。
> - Q:为什么需要工具调用? A:LLM 只能思考/生成,不能动数据;工具调用让 AI 通过代码执行真实操作——但执行权在业务层,模型只有声明权。
> - Q:RAG 如何接入? A:文档向量化 + top-k 检索 + 片段进 prompt,答案可溯源。

---

## 三、乐观锁与并发容错(难点 ③)

### 1. 并发从哪来(真实痛点)

一条需求同时被这几股力量改:负责人改字段、AI 多路工具并发回写、评审人留言。如果无脑直接写,会**互相覆盖**(A 存「风险低」,B 存「风险高」,后写把前面的盖掉)。最阴险的是——**没有报错,数据悄悄就丢了**,你根本不知道被覆盖过。

### 2. 悲观锁 vs 乐观锁

- **悲观锁**:改之前先锁记录,别人等,改完放。适合高频一起改,但会阻塞。
- **乐观锁**:不加锁排队,带 `version` 字段。写入时只要「你读的版本 == 当前版本」才准写,并 `version+1`;否则写失败,提示「别人改过了」。不阻塞他人,适合频繁协作、冲突不高的场景。

### 3. 项目真实实现


关键在 `UPDATE ... SET version=version+1 WHERE id=? AND version=?`:版本不匹配时**影响 0 行**,后端抛 `RequirementVersionConflictException`;全局 handler 统一响应,前端收到后提示并「以后端最新为准刷新」——**把「悄悄覆盖」变成「显式冲突 + 可恢复」**。

### 4. 冲突分级解决:不是所有 409 都该打断用户

抛出冲突只是第一步,更关键的是**拿到 409 之后怎么办**。直接弹「别人改过了,请刷新」是一种偷懒——因为它把两种完全不同的情况混为一谈:

- **状态类冲突**:version 是被投票、流程流转这类操作 bump 的,PRD 正文根本没动。这时拿新版本号重试,我的正文修改完整落库,不打扰任何人是正确做法;
- **正文类冲突**:别人真的改了正文,这时静默重写就是把对方的修改抹掉。

所以前端拿到 409 先做一次**正文 diff**(对比服务端最新正文和本地「服务端已知基线」):正文没变 → 静默重试;正文变了 → 进入章节级三方合并。

### 5. 章节级三方合并:借数据结构的力,不引入 CRDT

PRD 在系统里本来就是**结构化章节列表**(每章有 id/标题/正文),这个现成结构不用白不用。三方合并以「服务端已知基线」为共同祖先,把「我的编辑」和「服务端最新」按章节 id 对齐:

- **我改的章节 ∩ 对方改的章节 = 空** → 自动合并,双方的修改都保留,直接静默提交——大多数真实冲突(两人改不同章节)走这条路,**用户完全无感**;
- **双方改了同一章且内容不同** → 只把冲突章节挑出来,弹对比框「保留我的 / 用对方的」逐章裁决,其余章节照常自动合并;
- 对方新增的章节自动采纳,我删除的章节保持删除。

**为什么不直接上 CRDT/实时协同?** 因为这个产品的真实使用模式是「一人主导 + AI 辅助」,两人同时编辑本来就少,其中改同一章的更少。章节级合并用一个前端纯函数(带单测)解决了绝大多数冲突,工程量是 CRDT 的百分之一——**方案要匹配使用模式的频率,不为不存在的场景付过度成本**。

### 6. 其他容错(简历/答辩加分)

- **需求编号防重**:用 `req_sequence` 表 + `LAST_INSERT_ID(current_value+1)` 行级原子自增,并发创建不重号。
- **双存储降级**:MySQL 主存储,异常自动降级到 Redis 缓存(`saveToRedis`/`getFromRedis`),主库挂了关键数据不丢、页面还能读。
- **读口可降级**:读带 try-catch 兜底,新表查询失败保留旧字段,绝不因新功能坏了读口。
- **权限隔离**:列表非超管只返回 `owner=本人 或 member_accounts 命中`,防越权。

---

## 四、分层记忆系统 L0-L3(难点 ④)

### 1. 为什么需要记忆(真实痛点)

AI 生成 TRD 时怎么让它「懂这个用户」?最直接的做法是把历史 PRD 整段塞进 prompt 当背景。但这样有两个问题:

1. **贵**:注入段动辄几千字符(实测 4558 字符、user token 14211),每次生成都要付一遍。
2. **没记性**:用户跨需求的技术选型偏好(比如「缓存一律用 Redis」「审计要留 90 天」),AI 完全不记得,每个新需求都要人工重新交代。

> 类比:好助手会随身带一本「客户笔记」——不需要每次把客户过去一年的聊天记录全打印出来,翻笔记就够了。

**不解决会怎样**:上下文成本降不下来,而且 AI 永远是「金鱼记忆」,用户重复交代同样的事。

### 2. 怎么解决:三层记忆,各管一件事

- **L0 原始证据层**:会话、文档、操作记录原样落库,是所有上层记忆的来源,不加工。
- **L1 需求级原子记忆**:把一条需求的千字级 PRD 提炼成结构化短记忆(963 字符),**按 requirementId 隔离**——生成时只取本需求的记忆,不串。
- **L3 跨需求用户偏好**:从多个需求里提炼「稳定重复出现的模式」。**零模型成本**:关键词/归一化匹配(主题词表命中)→ 跨需求聚合 → 按 requirement 去重计频 → 频次阈值判定(**≥2 个需求命中 = candidate,≥3 个 = auto_confirmed**)。实测 4 个历史需求提炼出 8 条偏好(4 confirmed + 4 candidate)。注入 TRD 生成 prompt 开头,带指纹日志,**fail-open**——记忆服务挂了就当没有,绝不阻塞生成主链路。

(注:中间还规划过一层「需求画像」L2,后来评估发现它和 L1 聚合查询、L3 职责重叠,**主动砍掉了**——分层不是越多越好,每层必须有不可替代的信息,否则只会增加维护成本。最终就是干净的三层。)

### 3. 怎么证明有效:对照实验方法学(面试最出彩的部分)

记忆系统的效果不能靠「感觉变好了」,要有干净的对照实验。方法:

1. **指纹日志验证注入**:每条注入的记忆带唯一指纹写进日志,事后能证明「这次生成确实吃到了这段记忆」,而不是靠猜。
2. **强制新会话**:每次实验都开全新会话,排除 ChatMemory(会话内记忆)对结果的污染——否则你分不清是记忆系统起作用,还是上一轮对话残留。
3. **修复隐藏泄漏路径**:排查中发现 context JSON 会把 PRD 带进「关掉 PRD 注入」的实验组——**此前所有「关 PRD」的实验其实都没真正关掉**,结论全被污染。修复后才是干净对照。
4. **三重判据盲评**:LLM judge 盲评质量 + 关键词覆盖率 + 污染监测,不依赖单一主观打分。

**实验结论(可背的数字)**:
- 干净对照下,L1 结构化记忆(963 字符)替代 PRD 注入段(4558 字符):**token 节省 42%**(userTokens 14211 → 8246),产物体积几乎相同(22303 vs 22438 字节),**核心架构 100% 一致,质量损失仅 10-15%**——且损失集中在数值精度(如 99.9%/99.95% 被合并、90 天审计写成 180 天),是「可接受的模糊」,不是方向性错误。
- L3 注入对照:同需求 + 强制新会话下,**当 L1 存在且很全时,L3 注入的边际增益为 0**(四条偏好主题段落逐字一致)。

### 4. 从「负结果」到正确决策:L1 和 L3 的关系

L3 边际增益为 0 这个结果,一度让人想「那 L3 没用,砍掉/或改成和 L1 互斥注入」。但分析后发现:**L1 和 L3 信息正交**——L1 是「本需求的事实」(按 requirementId 隔离),L3 是「跨需求的稳定模式」(用户长期偏好)。本需求恰好覆盖偏好只是巧合,L1 薄或空的新需求初期 L3 才是唯一信息来源。所以**维持叠加注入**,互斥方案否决;同时明确了 L3 的真正价值场景(L1 薄/空的新需求),留待补充该场景的对照实验。

> 价值:面试可答——「一个实验结果为 0,你的第一反应是什么?」标准答案不是「删功能」,而是先问「实验场景有没有代表性」,再回到架构层面看信息是否正交。这正是 AI 工程里「用实验驱动决策」的完整闭环:发现增益 0 → 分析原因 → 修正模型假设 → 调整方案 → 定义下一步验证场景。

---

## 五、答辩时"用大白话讲"

| 技术点 | 一句话人话版 |
|-------|-----------|
| 状态机 | 把流程每一步用明确「动作表」表达,走一个动作不散落 if;后端是唯一权威,直接调接口也绕不过 |
| Agent 双层架构 | 不让一个大 Agent 自主管全流程:走哪条分支是代码定的(判错只影响一轮),ReAct 循环只在「改文档/查知识」两处开,而且工具是占位、执行权和落库权在业务层+人工确认——AI 最野的时候也被拴着 |
| Agent 工具调用 | AI 只能想不能动,ReAct(思考→调工具→看结果→再思考)让它真正改文档 |
| 结构化输出 | 强制 AI 返回能被 Java 直接解析的 JSON |
| 上下文预算 | 文档长了会超 token,提前压缩不丢信息 |
| 分层记忆 | L1 短记忆替代整段 PRD,上下文省 42%;L3 记住你跨需求的偏好,提炼不花一次模型调用 |
| 乐观锁 | 不加锁,靠版本号「你读的版本匹配才准写」,不匹配提示刷新,把覆盖变显式冲突 |
| 冲突分级+章节合并 | 冲突先分「改没改正文」:没改就悄悄重试;改了就按章节把两个人的修改拼起来,只有同一章都改了才弹框让人挑 |
| 双存储 | MySQL 为主,Redis 缓存兜底,写失败自动降级 |

---

## 附:深入代码入口

- 状态机动作:`WorkflowTransitionService.java`(switch + `guardTransition` + `returnForModify`)
- 乐观锁:`RequirementSnapshotService.java` 的 `updateWithOptimisticLock`、`nextRequirementId`
- Agent 核心:`agent/runtime/` 的 `SpringAiAgentRuntime`、`DocEditTools`、`ContextTokenGuard`、`IdeaIntentRouter`
- 分层记忆:`memory/` 包(L0 采集 / L1 需求级原子记忆 / L3 偏好提炼与注入,含指纹日志)
- RAG 检索:`SpringAiRagFacade`、`knowledge` 包
- 工具接入:`McpToolGateway`
- 前端冲突处理:前端「冲突后以后端最新为准刷新」

> 初读后端:建议顺着一条 Action(如 `PASS_REVIEW`)从 Controller → transition → confirmXxx → `updateWithOptimisticLock` 走一遍,就能把主链路串起来。